# nebula-core-v3 Project State

Last updated: 2026-07-12

## Current Shape

`/Users/lali/Projects/reddit` is a multilingual YouTube story-entertainment pipeline for the ChonkerTalks channel network.

Current content strategy: the old multilingual translated-Reddit pool has been replaced locally by explicit channel ownership. The full source of truth is [`topic-strategy-research-2026-07-10.md`](topic-strategy-research-2026-07-10.md).

- Each channel now has one unique `viewer_promise`, owned content bets, forbidden bets, an evidence policy, a render contract, and a gated cadence in `channels.json`.
- `acc1` owns Russian first-person horror sourced from Reddit; `acc2` high-concept internet case files; `acc3` digital-system explainers; `acc4` LATAM moral court; `acc5` human football stories; `acc6` skeptical web-mystery dossiers; `acc7` Italian social absurdity.
- Three production lanes are explicit: `reddit_horror_compilation` for `acc1`, `reddit_story_card` for complete first-person moral/social stories on the channels that own them, and `evidence_dossier` for factual explainers, real mysteries, internet timelines, and football.
- All seven channels are currently `automation_enabled=false` and `videos_per_day=0`. Numeric `topic_mix` arrays are marked candidate-scouting-only or superseded; none is an approved publishing weight.
- Twelve bounded Reddit-only reviews on 2026-07-10 proved source access but used the old committed config, so they did not validate the revised mixes.
- Twelve follow-up source-only GitHub reviews on 2026-07-11 checked the revised config from branch commit `ca6b0ad` across separate `week` and `month` windows. All runs passed credential/test/artifact enforcement, but the 63 queue entries collapsed to 19 unique posts and only two selected posts: one generic AITA story won eight runs and one link-dependent OutOfTheLoop question won four. No current `topic_mix` received `PASS`; numeric weights remain unapproved.
- The confirmed cross-channel contamination is now blocked locally with single-family validation gates, not claimed winner weights: `acc1=dark_curiosity`, `acc4=human_drama`, `acc5=football_culture`, and `acc7=visual_comedy`, each represented as `1.0` only so `channel_mix` cannot silently fall back to another channel's theme. These gates remain `automation_enabled=false` and require fresh source validation before any publishing interpretation.
- GitHub gate validation on commit `0b21d75` confirmed the routing correction. `acc4` returned only `human_drama` (10 complete week candidates, 7 month); `acc7` returned only `visual_comedy` (5 week, 12 month); `acc5` returned only `football_culture` (6 week, 7 month) but remained evidence-risk; `acc1` returned no eligible `dark_curiosity` Shorts candidate in either window and failed closed instead of substituting AITA. All GitHub unit-test and credential-preflight steps passed.
- The local Russian-channel follow-up adds an isolated `shorts_from_long` path for `acc1`: select a complete 2,800-25,000 character dark source, then condense it to at most 2,200 characters only through the explicit long-to-short adapter. The adapter fails unless exact source quotes cover setup, escalation, and payoff; other channels keep the original complete-short-source rule.
- Direct Reddit-horror competitor evidence changes `acc1` to 45-60 minute themed compilations: 3 complete `r/nosleep` fiction stories or 4-6 complete `r/LetsNotMeet` unverified encounters. Stories are translated and lightly edited without inventing events or expanding one post to fill the runtime.
- Two no-spend GitHub source reviews on commit `1206e39` tested that decision with `format_intent=long`: month run `29154057054` returned 8 eligible candidates and year run `29154057791` returned 11. All 19 were distinct, source bodies were present, and none depended on screenshots or images. The dark-story family therefore passes source availability, but automatic episode selection remains blocked: 18/19 candidates came from fiction-first `r/nosleep`, score separation was weak (all eight month candidates tied), and both selected stories ended with unresolved danger rather than a closed payoff. No numeric weight change is justified.
- Fresh long-source runs on commit `094f923` tested shorter discovery windows. Day run `29155023727` and week run `29155024651` both failed closed with no candidate meeting the current `1,000+ upvotes` and `2,800+ body characters` gates across bounded `r/nosleep` and `r/LetsNotMeet`. Long-form `acc1` scouting should therefore use `month/year` evergreen pools rather than depend on daily or weekly freshness. The existing 19-candidate pool clusters around forbidden rules in ordinary systems, family/home anomalies, night-work roles, public-space or travel traps, and haunted media/records. These are topic hypotheses, not cleared scripts or audience winners.
- User decision: every `acc1` story is sourced from Reddit. Two bounded internal artifact-only automation tests may use uncleared candidates when their manifests say `rights_mode=test_only_not_cleared` and `publication_authorized=false`; no upload or publication is allowed. Distribution still requires the Reddit author's required commercial translation/adaptation/narration/audiovisual rights. The canonical sourcing and ownership contract is [`russian-horror-editorial-system.md`](russian-horror-editorial-system.md).
- The present `acc1` code path does not implement that contract. The long producer gate sees only the first 1,800 characters; the adapter cleans rather than writes a scene-based Russian episode; the next Gemini call preserves the Reddit sequence as a translation; AI33 reads that result; and the renderer remains a widescreen Reddit-card slideshow. There is no rights manifest, truth-mode artifact, separate Russian writer/editor, continuity review, manual script acceptance, fiction-disclosure QA, or scene-based long-form storyboard. `acc1` remains blocked before paid production even though source availability passed.
- The isolated artifact-only lane under `specs/acc1-automated-reddit-pilot/` now targets compilations. The local implementation includes bounded same-lane 3-6 story selection with a 40-70 minute source estimate; full-story-first Gemini translation at 16,384 output tokens with atomic chunk fallback/resume, independent review and two-revision ceiling; ordered narration preflight; chunked/idempotent AI33 `eleven_v3` state with atomic task-id persistence and poll-only resume; safe Reddit-image capture/download; one `gpt-image-2` fallback visual per story; local-only 1920x1080 H.264/AAC rendering; compilation metadata; fail-closed QA; and the dedicated artifact-only `.github/workflows/acc1_compilation_pilot.yml`. No complete provider-to-MP4 run has passed yet.
- GitHub carrier run `29181179414` on commit `174f73f` failed in unit tests before Reddit/provider access because Pillow was installed after tests; no provider step ran. The dependency order was fixed in `7ebbfd1`.
- Retry run `29181212503` passed all deterministic tests, Reddit credential preflight, bounded `r/nosleep` fetch, topic review, and three-story compilation selection. It selected 7,982 source words (estimated 61.4 minutes): posts `1u8m7zi`, `1ul0dpq`, and `1u73jt5`. The first Gemini full-story translation returned a long but truncated JSON object after about 170 seconds; GPT Image 2, AI33, render, and QA were skipped. Artifact `reddit-source-smoke-acc1-29181212503` is downloaded at `/tmp/acc1-run-29181212503/`.
- The confirmed routing bug was that truncated/non-JSON provider output bypassed chunk fallback because it surfaced as `VectorEngineError` rather than a parsed `complete=false`. The fix classifies only truncation/empty/JSON-parse errors as incomplete translation and preserves auth/rate failures as terminal.
- Approved retry `29181535692` on commit `73575e2` reached chunk-fallback translation but GitHub cancelled the job at its configured 10-minute ceiling (`The operation was canceled`). The artifact `reddit-source-smoke-acc1-29181535692` contains the source/selection evidence but no translation checkpoint, image, TTS, render, or QA output. The run therefore spent only Gemini translation calls; GPT Image 2 and AI33 were skipped. Root cause is orchestration durability: the carrier job timeout was too short and translation persisted only after a complete story. Follow-up commit `bd3f7eb` raises the carrier ceiling to 90 minutes and atomically checkpoints the glossary plus every completed translation chunk, resuming directly from saved chunks without repeating the failed full-story call. It passes 76 local unit tests and syntax/diff checks, is pushed to `codex/reddit-topic-source-review-20260711`, and has not been retried live. Another provider-spend retry requires explicit approval.
- Approved run `29182609994` on commit `586bcad` verified the 90-minute timeout and atomic chunk persistence. It reached the first story's independent review, then failed closed because the reviewer still returned `REVISE` after the two-revision ceiling. Artifact `reddit-source-smoke-acc1-29182609994`, downloaded at `/tmp/acc1-run-29182609994/`, preserves the first story's glossary and all four translated chunks (3,321 Russian words). GPT Image 2, AI33, render, QA, and YouTube did not run. The follow-up adds an explicit `resume_artifact_run_id` input, source-hash-validated restore of prior translation checkpoints, and a review-decision checkpoint so a later approved run can reuse the paid chunks and preserve reviewer issue evidence. This follow-up passes 77 local tests but is not live-verified; no further paid retry is authorized yet.
- Approved resume run `29183112871` on commit `191ee7e` live-verified cross-run artifact restoration and source-hash compatibility. Story `1u8m7zi` passed review after one revision and story `1ul0dpq` passed without revision; both complete story checkpoints were preserved. Story `1u73jt5` preserved its ending in all three reviews but remained `REVISE` after the two-revision ceiling because each full-document rewrite introduced or exposed different local wording defects. Artifact `reddit-source-smoke-acc1-29183112871`, downloaded at `/tmp/acc1-run-29183112871/`, contains all review decisions and issues. GPT Image 2, AI33, render, QA, and YouTube did not run. The local follow-up changes reviewer output to source-backed exact quote/replacement patches, applies only unique local replacements instead of regenerating the entire translation, and stores the current translated document in schema-v2 review state for later resume. This is locally tested but not live-verified; no further paid retry is authorized yet.
- Approved run `29183388036` on commit `89d55d7` passed all three translations/reviews, compilation packaging, three GPT Image 2 generations, and the storyboard. It stopped before any AI33 request because narration preflight correctly blocked raw clock time `3:15`; render, final QA, and YouTube did not run. Artifact `reddit-source-smoke-acc1-29183388036`, downloaded at `/tmp/acc1-run-29183388036/`, contains the accepted scripts, metadata, three images, and eight-slide storyboard. Visual inspection accepted images 2 and 3 but rejected image 1 because it contains an English written rules list despite the no-text prompt. The local follow-up converts valid Russian `HH:MM` tokens to deterministic spoken forms, restores checksum/prompt-matched prior images, and adds a stronger no-written-props guard only for rules/time concepts. A later approved resume should therefore reuse images 2/3 and regenerate only image 1. This passes 80 local tests but is not live-verified; no further paid retry is authorized yet.
- Approved run `29183978019` on commit `170a77d` live-verified clock-time normalization, selective image reuse, and regeneration of only rejected image 1. Visual inspection accepted the replacement: it has no written list, English text, or readable clock face. All translations, packaging, three images, and storyboard passed. AI33 accepted two `eleven_v3` tasks: the first chunk completed with a checksum-bound MP3, while polling the second saved task ID returned HTTP 500 with `retryable=true`; 14 chunks were never submitted. Render, final QA, and YouTube did not run. Artifact `reddit-source-smoke-acc1-29183978019`, downloaded at `/tmp/acc1-run-29183978019/`, preserves the 16-chunk TTS plan, one COMPLETE chunk, one SUBMITTED task, and the finished audio file. The local follow-up restores TTS state/audio cross-run and retries transient AI33 polling errors with bounded backoff without resubmitting saved task IDs. This passes 81 local tests but is not live-verified; no further paid retry is authorized yet.
- Approved run `29184260452` on commit `37b2a4e` is the first complete provider-to-MP4 artifact-only pass. All three source-preserving translations/reviews, packaging, checksum-bound images, eight-slide storyboard, 16 AI33 `eleven_v3` chunks, audio concatenation, 1920x1080 H.264/AAC render, and fail-closed compilation QA passed. The final video is 3,253.777 seconds (54:13), 122,419,630 bytes, H.264 video plus 44.1 kHz stereo AAC; QA reports `PASS`, zero failures/warnings, three stories, eight slides, and a 51.79-minute source estimate. Artifact `reddit-source-smoke-acc1-29184260452` is downloaded at `/tmp/acc1-run-29184260452/`. No YouTube or publication step ran. Frame inspection at intro, story sections, and outro found clean 16:9 composition and readable title/CTA, but also proved the current visual lane is not publication-ready: only eight mostly static slides cover 54 minutes. Pipeline durability and technical artifact integrity have reached A-range evidence; visual storytelling/retention remains BLOCKED below A- until the storyboard has scene-level visual cadence and actual motion/shot variation.
- Approved run `29186076815` on commit `116be13` live-verified the bounded emotion-video proof with exactly one acc1 narrator AI33 request. It used `eleven_v3`, emotional tags, creative settings, and `with_transcript=true`; YouTube mapping was skipped and no Reddit, Gemini, Image2, render-publish, or upload path ran. AI33 returned 52 usable timed words from 0.14s to 24.399s, so the 24.8-second 1920x1080 H.264/AAC proof reports `timing_source=ai33`, not an estimated fallback, with seven caption phrases. GitHub artifacts are downloaded at `/tmp/acc1-emotion-run-29186076815/`. Frame inspection found the first render too dark and one phrase split poorly; a no-spend local rerender now uses a lighter grade, smaller captions, and punctuation-aware phrase boundaries. The improved MP4 is `/tmp/acc1-emotion-run-29186076815/improved/emotion-video-sample.mp4`. Local tests now pass 84/84; the improved renderer has not yet been re-run in GitHub, but it uses the exact downloaded AI33 audio/timings and adds no provider dependency.
- Bounded AI33 alternate-narrator run `29190123861` on commit `7efa7bc` generated one Russian emotional sample with David (`elevenlabs_cCYjmrGZaI86GUJ7F2Nn`), explicit `eleven_v3`, the `creative` profile, and provider word timings. The MP3 is 27.951 seconds, 44.1 kHz stereo and is downloaded under `/tmp/ai33-david-29190123861/voice/`; the paired local-format proof is under `/tmp/ai33-david-29190123861/video/`. YouTube mapping was disabled and no Gemini, Reddit or publication path ran. Failed precursor run `29190054352` stopped at AI33 validation before audio generation because metadata returned a raw ElevenLabs ID without the provider prefix; the manual-sample override now validates IDs and adds `elevenlabs_` for raw catalog IDs without changing `channels.json`. This sample is listening evidence only and does not change the configured acc1 narrator.
- User-selected Russian voice candidates are `elevenlabs_MOgsVr0EwwxqQs5cNDhu` (male) and `elevenlabs_JBFqnCBsd6RMkjVDRZzb` (female). Their approved listening contract is unmodified provider speed `1.0` and narration text without Eleven v3 emotion/audio tags. The earlier `1.10x`/`1.20x` local files and tagged samples are comparison artifacts only, not production settings. Roles and `channels.json` remain unchanged until a separate selection/assignment decision.
- GitHub run `29217400229` on commit `aeb83ef` generated one bounded plain-horror sample for male candidate `elevenlabs_MOgsVr0EwwxqQs5cNDhu` with explicit `eleven_v3`, provider speed `1.0`, default voice settings, no emotion/audio tags, and usable AI33 word timings. YouTube mapping, Gemini and Reddit were not run. The workflow's optional still-image proof failed after TTS because Python 3.10 rejected a backslash inside an f-string expression; the compatibility fix is local and tested without another provider call. The downloaded voice artifact is under `/tmp/chonker-mogs-run-29217400229/`. A local 24.1-second 1920x1080 H.264/AAC proof loops the user-supplied Chonker MP4, ignores its audio, uses only the new AI33 voice, and constrains Reddit Sans text to a 38-character left column so it does not cover the mascot. The accepted visual proof is `/tmp/chonker-mogs-loop-proof-v2/emotion-video-sample.mp4`; this remains a format/voice test and does not change `channels.json`.
- A second no-spend local render of the same verified audio/timings implements the selected Reddit-on-background contract without a card or opaque panel: subreddit/author metadata and the post title appear only on page one; timed body phrases accumulate directly on the moving background; a full page fades out and continuation text starts on a clean page without repeating the title; vote/comment/share/save actions appear only during the final 3.2 seconds. The test MP4 is `/tmp/acc1-emotion-run-29186076815/reddit-pages/emotion-video-sample.mp4`. Frame inspection at 2s, 7s, 13s, and 22.5s confirms progressive fill, title-free continuation, direct-background readability, and end-only actions. This renderer mode is local-verified and uses no additional provider calls.
- Direct browser inspection of Saranago Boy video `PMnk1PkrsO0` at 0:30, 0:32, 0:34, 2:00, 10:00, 20:00, and 30:50 established the selected competitor-like geometry: a wide 16:9 comment area, roughly 8-11 light-gray lines on a full page, fixed precomputed wrapping with progressive word chunks and no whole-text fade, and avatar/username/time on the first page. The current no-spend v5.1 uses official Reddit Sans at meta/title/body/action sizes 28/46/40/27 px, 80 px side margins, 76-character lines, 700/900-character page capacities, and a one-pixel readability outline. Per the user-selected variant, the thin upvote/count/downvote, reply, share, and overflow footer appears only during the final 3.2 seconds rather than persistently like the competitor. The locally rendered proof is `/tmp/acc1-emotion-run-29186076815/reddit-competitor-v5-final/emotion-video-sample.mp4`; its JSON report records the exact layout contract. This remains a format proof, not audience-retention evidence.
- The pilot specification now includes a required audience-learning loop rather than treating upload volume as success. Each episode will carry an experiment/archetype manifest, three honest packaging angles, scene time ranges, immutable 24h/72h/7d/28d analytics snapshots, and scene-level retention mapping. Initial targets remain relative to the first six same-channel long-form pilots; an archetype is not promoted from one outlier and needs at least two wins across three distinct stories. Analytics ingestion and YouTube/Studio readback are specified but not implemented or live-verified yet.
- `acc4` and `acc7` remain the Reddit-card pilot candidates. `acc1` requires the rights-cleared Reddit-horror lane; `acc2`, `acc3`, `acc5`, and `acc6` require the evidence-dossier lane before publishing.

- `index.html`, `style.css`, `app.js` implement the local RedditSim recorder UI.
- `scraper.py` fetches candidate Reddit stories through PRAW OAuth2, weighted topic-family source planning, network-wide duplicate/signature/similarity guards, channel `topic_exclusions`, velocity scoring, topic-fatigue penalties, and a bounded Gemini producer-quality gate, then writes `story_data.json` and `producer_queue.json`. Disabled channels fail before Reddit access unless an approved review passes `--allow-disabled-channel`; cross-channel reuse requires the explicit `--allow-cross-channel-reuse` escape hatch. The producer prompt now prefers each channel's `viewer_promise` and `producer_brief` over language-wide defaults.
- `scraper.py` now requires explicit `REDDIT_CLIENT_ID` and `REDDIT_CLIENT_SECRET` environment credentials; `REDDIT_USERNAME` and `REDDIT_PASSWORD` are an optional all-or-nothing script-OAuth pair. It never falls back to credential material embedded in source. `--check-reddit-config` is a no-network preflight and `--no-save-history` keeps bounded candidate reviews from changing `published_history.json`.
- `story_adapter.py` is the selected-story editorial layer. It calls the shared Gemini text provider after scraper selection and before metadata/translation, tightens the story without inventing facts, can move a source-backed hook into the opening, preserves URLs/timeline/point of view, and fails in strict mode when hook evidence is not found in the source title/body/comment text.
- For `acc1` Shorts only, `story_adapter.py --long-to-short --max-body-chars 2200` may condense a complete long dark source. This is not raw truncation: it requires source-backed setup/escalation/payoff evidence and rejects an over-limit or unsupported retelling.
- `translator_tts.py` localizes non-English `story_data.json` through the shared Gemini text provider, keeps translated display fields for storyboard/card rendering, prepares separate narration-only fields where raw URLs are spoken as localized on-screen-link phrases and Russian numeric tokens are expanded into spoken words, then submits narration text to AI33 TTS v3. Full-story translation now allows 16,384 output tokens by default instead of 4,096; override it with `--translation-max-output-tokens` or `GEMINI_TRANSLATION_MAX_OUTPUT_TOKENS`. Channels can use separate `tts_voice` and `comment_tts_voice` roles; the script concatenates role segments into one `narration.mp3`. The higher live response size has not been provider-tested in this change.
- `metadata_generator.py` generates YouTube packaging metadata through the shared Gemini text provider. It now treats tags as secondary, asks for three honest title/thumbnail/first-screen options, stores them in `packaging_options`, and writes the selected option into backward-compatible title/thumbnail fields. It also has an explicit `--fallback-on-error` mode for render dry-runs: if Gemini blocks the metadata prompt, it writes conservative marked fallback metadata so MP4/audio/render QA can still run. Production upload workflows should stay fail-closed unless this flag is intentionally added.
- `thumbnail_generator.py` can generate a thumbnail image through VectorEngine image generation, but only with explicit `--confirm-spend`.
- `storyboard_generator.py` creates a deterministic no-API `storyboard.json` from `story_data.json`. It now emits `render_slides`: story text is split into screen-sized chunks, first story chunks show header/title but no footer when the story continues, middle story chunks show only continuation text, and only the final story chunk shows post upvotes/comments/share. Comment continuations follow the same rule for comment header/footer. Long stories advance by replacing the card content rather than scrolling, and short final orphan chunks are merged back when they fit.
- `render.py` opens the existing RedditSim UI in headless Chrome/Chromium, captures deterministic clean slide screenshots, and uses FFmpeg to render an MP4 from `storyboard.json`. In default `--orientation auto` mode, videos up to 180 seconds render vertical 9:16 (`1080x1920`, mobile layout), while videos longer than 180 seconds render horizontal 16:9 (`1920x1080`, desktop layout). Both orientations render clean Reddit cards with editor/sidebar widgets hidden. If `narration.mp3` exists, it merges that file as an AAC audio track. Current workflows pass `--no-karaoke`, so transcript timings are used only to switch static slides at `word_start` boundaries; no yellow karaoke highlight is rendered. If usable timings are unavailable, the renderer falls back to word-weighted slide boundaries. `--report render_report.json` writes render readback for the QA gate.
- `pre_publish_qa.py` is the fail-closed local/upload gate. It checks final MP4 audio/video streams, audio/video duration match, render report state, story adaptation, hook evidence, narration URL sanitization, storyboard slides, metadata title length, and channel language before upload. Karaoke checks run only when explicitly requested with `--require-karaoke`.
- `scripts/move-to-trash.sh` is the project safe-trash helper. Generated previews and scratch files must be moved to `Trash/<timestamp>/...`; agents must not use direct deletion commands.
- `uploader.py` is a YouTube Data API uploader with a fail-closed channel-token preflight against `channels.json` and post-upload metadata readback.
- `.github/workflows/auto_publish.yml` runs the cloud publish pipeline. It verifies the YouTube refresh token against `channels.json` before Reddit/Gemini/AI33/render work. The first full live smoke succeeded as an unlisted upload; the later OAuth/channel-mapping blocker is reported resolved by the user as of 2026-07-02, but the fail-closed preflight remains mandatory before any content-generation spend or upload.
- `.github/workflows/video_dry_run.yml` is currently a live manual workflow: it fetches Reddit content, can spend Gemini/AI33 quota or credits for quality/adaptation/metadata/localization/TTS, renders `final_output.mp4` with clean no-karaoke visuals, runs `pre_publish_qa.py`, and uploads story, producer queue, metadata, audio, transcript, render report, QA report, video, and previews. Manual dry-runs now support `topic_family`, `video_slot`, `content_format`, and `voice_settings_profile`; use `content_format=shorts` to exercise live search/filtering while forcing a vertical artifact without YouTube upload, and use `voice_settings_profile=creative` only for intentional expressive-voice tests.
- `.github/workflows/reddit_source_smoke.yml` supports forced family/time/source bounds, `format_intent`, and a review label, preserves bounded queue source bodies, and records git SHA, `channels.json` SHA-256, exact inputs, and a config snapshot. It still forces AI quality off, writes no history, and calls no Gemini, AI33, renderer, or YouTube path. The long-form review scope is committed and pushed on branch `codex/reddit-topic-source-review-20260711` at `1206e39`; `origin/main` is unchanged.
- The follow-up adds `scripts/review_reddit_topics.py` to that source-review lane. It reads complete queued bodies, classifies title-visible `acc1` archetypes, records fiction/unverified/evidence truth mode, flags source dependency, series/open-ending and target-runtime risks, and returns three diverse `SHORTLIST_FOR_RIGHTS_REVIEW` examples in `topic-review.json`. It never authorizes production. The workflow input now permits up to four bounded subreddits; live four-source GitHub validation has not run.

## GitHub Dry-Run Status

- Dry-run renderer commits were pushed to `origin/main` and transferred to `webpot-ru/nebula-core-v3`.
- GitHub Actions run `28421055129` succeeded on 2026-06-30 under the `webpot-ru` account.
- The `render-dry-run` job completed in 1m 21s, passed validation tests, and successfully uploaded the `chonkertalks-dry-run-video` artifact.
- The artifact was downloaded locally to `build/render/chonkertalks-dry-run-video` and contains `final_output.mp4` (594 KB, verified codec and resolution).
- The transition from `startup_failure` to success was resolved by migrating repository ownership to `webpot-ru` (which has a healthy billing profile) and setting all required repository secrets.
- Manual `video_dry_run.yml` run `28699752190` succeeded on 2026-07-04 for `acc1`, `time_filter=auto`, `topic_family=channel_mix`, `video_slot=1`, and `content_format=shorts`. It selected `r/nosleep` post `1uh3bc5` ("There's an invisible line in my town that no animal will cross."), localized/adapted it to Russian as "В нашем городе есть невидимая линия, которую не перейдет ни одно животное", generated `narration.mp3`, rendered a vertical Shorts MP4, and uploaded artifact `chonker-test-acc1`.
- The run `28699752190` artifact was downloaded locally to `build/github-artifacts/run_28699752190/chonker-test-acc1`. `pre_publish_qa.json` passed with `status=ok`, `0` failures, and `0` warnings. `ffprobe` verified `final_output.mp4` as H.264 `1080x1920` at 30 fps with AAC audio; video duration was `30.700s`, audio duration was `30.682s`, and QA measured audio/video mismatch at about `0.006s`.
- `render_report.json` for run `28699752190` confirmed `renderFormat=shorts`, `resolution=1080x1920`, `audio=narration.mp3`, `frameSchedule=uniform_progress`, and `karaokeEnabled=false`. The artifact contains no `narration.json`. Visual preview readback (`preview_01.png`, `preview_mid.png`, `preview_end.png`) showed clean static Reddit cards with no karaoke highlight and no editor/sidebar controls; the post footer appears only in the final preview frame.
- Manual `video_dry_run.yml` run `28703717316` succeeded on 2026-07-04 for `acc1`, `time_filter=auto`, `topic_family=channel_mix`, `video_slot=1`, and `content_format=shorts` after the source-length / full-body quality-gate fixes. It selected `r/AITAH` post `1uh7cbj` and localized/adapted it to Russian as "Сдаю плазму ради аренды, пока падчерица копит на бэби-шауэр". The selected source body was `2301` chars under the Shorts policy `250-2400`, with `content_limits=null`, so the story was selected as a complete short source rather than trimmed.
- The run `28703717316` artifact was downloaded locally to `build/github-artifacts/run_28703717316/chonker-test-acc1`. `pre_publish_qa.json` passed with `status=ok`, `0` failures, and `0` warnings. `ffprobe` verified `final_output.mp4` as H.264 `1080x1920` at 30 fps with AAC audio; video duration was `96.866667s`, audio duration was `96.862041s`, and QA measured audio/video mismatch at about `0.0046s`. `render_report.json` confirmed `renderFormat=shorts`, `resolution=1080x1920`, `audio=narration.mp3`, `frameSchedule=uniform_progress`, and `karaokeEnabled=false`; the artifact contains no `narration.json` by design.
- Visual readback for run `28703717316` showed clean static Reddit cards with no karaoke highlight and no editor/sidebar/topbar controls. Direct MP4 frame extraction at `45s` showed the second story slide without header/footer, at `70s` showed the final story slide, and `preview_end.png` showed the post footer metrics only at the end. Metadata and localization readback showed `source=google-gemini`, `language=ru`, and `comments=0` for this Shorts run.

## Live Publish Smoke Status

- Manual `auto_publish.yml` run `28441721783` succeeded on 2026-06-30 for `acc4`, `video_slot=1`, `time_filter=auto`, `topic_family=human_drama`, `privacy_status=unlisted`.
- The selected story was `r/AmItheAsshole` / `top/week`: "AITA for being blunt with my boyfriend about why he isn't getting hire..." with about 7.3k upvotes and 1.5k comments. Gemini quality gate approved it as `REWRITE` after skipping one malformed-JSON quality response.
- Localization/TTS succeeded using the old Spanish Edge-placeholder voice that was configured at the time; this was before the channel voice migration to ElevenLabs. `story_data.json` was localized to Spanish (`es-419`), `narration.mp3` was produced, and `narration.json` was saved.
- Render succeeded, but exposed a format bug: `final_output.mp4` was 1080x1920 for a 237.672s video. Videos longer than 180 seconds should be horizontal, so the renderer now switches `--orientation auto` outputs over 180 seconds to 1920x1080.
- Upload succeeded as unlisted with YouTube video id `cFX2tZmLrAs`; public oEmbed readback returned the Spanish title "¿Soy la mala por decirle a mi novio la verdad de por qué nadie lo contrata?".
- Historical blocker: public/user readback showed videos landing on the wrong channel for the requested account. The user reports that all seven OAuth credentials and scopes have now been reissued and verified, so this is no longer the active blocker; keep `uploader.py` channel preflight enabled so any future mismatch fails before spend/upload.
- The workflow committed the published-history update to `origin/main` as `d54da14 chore: history [acc4] slot=1 [skip ci]`.
- Manual `auto_publish.yml` run `28693621091` succeeded on 2026-07-04 for `acc1`, `video_slot=1`, `time_filter=auto`, `content_format=shorts`, `privacy_status=unlisted`. It uploaded to the verified `ChonkerTalks Россия` account as video id `LgURZlQqj10`; upload readback reported `privacy=unlisted`, `language=ru`, and channel id `UCNSxg53AGM4WstRjGiQdS8w`. Render report showed 45.531s vertical Shorts output (`1080x1920`), AAC audio from `narration.mp3`, `karaokeEnabled=true`, `transcriptWordCount=115`, and `frameSchedule=karaoke_phrase_times`. `pre_publish_qa.py` passed with `0` failures, `0` warnings, and karaoke coverage `0.9914`. The workflow committed the published-history update to `origin/main` as `c023eb9 chore: history [acc1] slot=1 [skip ci]`.

## Verified Locally

- On 2026-07-10, `python3 -m unittest tests/test_scraper_reddit_config.py tests/test_channel_strategy.py` passed all 10 tests. The tests cover secret-free Reddit credential wiring, unique channel promises, fail-closed strategy fields, topic-mix normalization, config-driven producer briefs, disabled-channel preflight, and network-wide duplicate protection.
- `python3 -B -m py_compile scraper.py story_adapter.py metadata_generator.py` passed on 2026-07-10.
- `channels.json` parses as valid JSON. All seven viewer promises are non-empty and unique, all channels are automation-disabled, and every candidate-scouting mix still sums to 1.0 while being explicitly marked unvalidated or superseded.
- `node --check app.js` and `python3 -B -m py_compile storyboard_generator.py render.py translator_tts.py pre_publish_qa.py` passed on 2026-07-04 after the static timed-slide / no-karaoke render update.
- Browser localhost QA passed on 2026-07-04 for the deferred final post footer in both target render sizes: vertical 1080x1920 mobile layout and horizontal 1920x1080 desktop layout. In both checks, the final story slide started with `meta-deferred` / hidden footer and changed to `meta-revealed` / visible footer on the final karaoke phrase; editor/sidebar/nav controls were hidden and browser console logs were clean.
- `python3 -m py_compile storyboard_generator.py render.py translator_tts.py scraper.py` passed on 2026-07-02.
- `python3 storyboard_generator.py --input sample_story_data.json --output build/render/slide_storyboard.json` passed on 2026-07-02 and produced 4 slide-backed scenes from the safe sample fixture.
- `python3 render.py --storyboard storyboard.json --output final_output.mp4` passed once locally through the RedditSim Chrome DevTools path and captured 8 simulator frames.
- A 2026-07-04 local no-spend render using `build/github-artifacts/run_28703717316/chonker-test-acc1/story_data.json` plus the existing `narration.mp3` passed after the timed-slide/background update: `/tmp/reddit_timed_static_bg.mp4` rendered as H.264 `1080x1920` with AAC audio, `durationSec=96.862041`, `karaokeEnabled=false`, `frameSchedule=static_slide_boundaries`, `slideTimingSource=word_weighted`, and two story slides. Extracted frames at 1s, 48s, and 90.2s showed wider Reddit-like typography, denser card usage, subtle dark grain/vignette background, no editor controls, no karaoke highlight, and the footer only at the final reveal.
- A synthetic transcript timing check on 2026-07-04 confirmed that when `narration.json` word timings are present, `render.static_slide_frame_plan()` uses the next slide's `word_start` timestamp as the static slide boundary and reports `slideTimingSource=transcript`.
- A synthetic AI33 subtitle payload check on 2026-07-04 confirmed that inline SRT/VTT text is parsed into `narration.json` words with `timing_source=subtitle`, `timing_status=ok`, and usable `start`/`end` values for render slide timing. This check did not call live AI33 or spend provider credits.
- FFmpeg encoding was independently verified from the captured RedditSim frames: H.264, 1080x1920, 30 fps, 22.0s, 660 frames.
- `test -s final_output.mp4` passed on local generated artifacts, and the current GitHub no-karaoke artifact `build/github-artifacts/run_28699752190/chonker-test-acc1/final_output.mp4` was downloaded and verified with `ffprobe`.
- `app.js` passes `node --check` on 2026-07-02.
- RedditSim loaded in a real browser at `http://localhost:8080/`; the typewriter animation completed and rendered title, body, and comments.
- Browser console only showed missing `favicon.ico` 404s during that smoke, not JavaScript runtime errors.

## AI33 TTS State

- `translator_tts.py` now uses `POST https://api.ai33.pro/v3/text-to-speech` with multipart FormData and `xi-api-key`.
- `translator_tts.py` sends `model_id=eleven_v3` by default; override with `--model-id` or `AI33_TTS_MODEL_ID` only intentionally.
- The local TTS model contract is now fail-closed: `--require-model-id eleven_v3` rejects a different requested model before any AI33 call, every successful/submission-only request writes sanitized `tts_request_metadata.json`, and `narration.json` records `requested_model_id`. If AI33 explicitly reports a different model in its task payload, generation fails after preserving the mismatch metadata. When AI33 does not report a model, the artifact says `provider_not_reported` rather than claiming server-side confirmation.
- `auto_publish.yml` and `video_dry_run.yml` pass `--model-id eleven_v3 --require-model-id eleven_v3` explicitly. `pre_publish_qa.py --require-tts-model eleven_v3` requires the safe audit artifact, and the dry-run artifact includes it. The model preflight/sample path was verified by paid GitHub AI33 A/B runs `29153420437` (`default`) and `29153423227` (`creative`) on commit `a810197`.
- `translator_tts.py` can pass optional AI33/ElevenLabs `voice_settings` through `--voice-settings-json` or `AI33_VOICE_SETTINGS_JSON`. This is intentionally off by default until short sound tests confirm that the chosen settings improve expression without hurting word timings.
- `scripts/generate_ai33_voice_samples.py` now supports emotion-focused sound tests without changing production publishing: `--text-style emotional` adds sparse Eleven v3 audio tags such as `[curious]`, `[sighs]`, and `[whispers]`; `--voice-settings-profile creative` sends `{"stability":0.35,"similarity_boost":0.75,"style":0}` for a more expressive first pass; `natural` uses stability `0.50`; `robust` uses stability `0.70`; `default` omits provider settings. Dry-run mode prints the exact sample payloads without spending credits.
- The expected secret is `AI33_API_KEY`; `A133_API_KEY` is accepted only as a compatibility fallback for older local notes.
- `channels.json` now uses ElevenLabs-prefixed AI33 voice ids for all seven channels. Edge voice ids remain only historical placeholders and should not be used for production publishing.
- Raw ElevenLabs candidate ids provided from AI33 Voice Library screenshots/readback must be written with AI33 prefixes in this repo, for example `elevenlabs_cCYjmrGZaI86GUJ7F2Nn`. Currently documented candidates include active pairs for Russian, English, German, LATAM Spanish, Brazil Portuguese, French, and Italian, plus spare/alternate voices.
- Voice selection is per channel and per role; there is no need to find one universal voice for every language. `acc4` now has a Spanish Latin-accent narrator/comment pair in `channels.json`: `elevenlabs_22VndfJPBU7AZORAZZTT` for title/body narration and `elevenlabs_8mBRP99B2Ng2QwsJMFQl` for comments. AI33 metadata readback confirmed both as Spanish `es-AR` with `latin american` accent. This still needs a short AI33 sound test before public use.
- `acc5` now has a Brazilian-accent narrator/comment pair in `channels.json`: `elevenlabs_dX7gRq1dIvLTgUaWpEFn` for title/body narration and `elevenlabs_4r3G9XKliGgVZLKMgjik` for comments. AI33 metadata readback confirmed both as Portuguese `pt-BR` with `brazilian` accent. This still needs a short AI33 sound test before public use.
- `acc1` now has a standard Russian narrator/comment pair in `channels.json`: `elevenlabs_rQOBu7YxCDxGiFdTm28w` for title/body narration and `elevenlabs_ymDCYd8puC7gYjxIamPt` for comments. AI33 metadata readback confirmed both as Russian `ru-RU` with `standard` accent. This still needs a short AI33 sound test before public use.
- `acc3` now has a standard German narrator/comment pair in `channels.json`: `elevenlabs_aTTiK3YzK3dXETpuDE2h` for title/body narration and `elevenlabs_LB5G0Z4EP98YaEgL654m` for comments. AI33 metadata readback confirmed both as German `de-DE` with `standard` accent. `elevenlabs_5KvpaGteYkNayiswuX2h` is documented as a spare older German male voice. This still needs a short AI33 sound test before public use.
- `acc2` now has a US English narrator/comment pair in `channels.json`: `elevenlabs_sB7vwSCyX0tQmU24cW2C` for title/body narration and `elevenlabs_DODLEQrClDo8wCz460ld` for comments. AI33 metadata readback confirmed both as English `en-US` with `american` accent. `elevenlabs_nzFihrBIvB34imQBuxub` is documented as a spare young English male voice. This still needs a short AI33 sound test before public use.
- `acc6` now has a France-standard French narrator/comment pair in `channels.json`: `elevenlabs_wufFsVwuYBePWKO6dMMN` for title/body narration and `elevenlabs_i6ke7jvmGEVUyV4zjSaT` for comments. AI33 metadata readback confirmed `wuf...` as French `fr-FR` standard male and `i6...` as French `fr-FR` Parisian female. `elevenlabs_93nuHbke4dTER9x2pDwE` remains a Québec male spare. This still needs a short AI33 sound test before public use.
- `acc7` now has a standard Italian narrator/comment pair in `channels.json`: `elevenlabs_ImsA1Fn5TNc843fFdz99` for title/body narration and `elevenlabs_RXoaSpLaWTEckJgPUBG3` for comments. AI33 metadata readback confirmed both as Italian `it-IT` with `standard` accent. This still needs a short AI33 sound test before public use.
- `.github/workflows/voice_metadata_check.yml` and `scripts/check_ai33_voice_metadata.py` provide a no-audio readback path for AI33 voice metadata through the repository `AI33_API_KEY` secret. The workflow uses `GET /v3/voices?provider=elevenlabs&search=<voice_id>`, prints only sanitized metadata for requested voice IDs, and must not call `/v3/text-to-speech`.
- Current remaining voice coverage: all seven channels have narrator/comment pairs configured. Remaining blocker is sound quality verification, not missing catalog coverage.
- Live AI33 v3 smoke passed on 2026-06-29 using the gitignored LUNA2 local env key as a one-off user-approved read. The secret was not printed or copied into this repo.
- The first smoke submitted an ElevenLabs-prefixed voice id (`elevenlabs_21m00Tcm4TlvDq8ikWAM`) with text containing `[sighs]`, `[laughs]`, and `[whispers]`; AI33 returned `task_id=3f489e0b-3b73-40c2-95fd-071ee694055c`, task polling returned `status=done`, and `/tmp/reddit_ai33_laugh_sigh.mp3` was written.
- A second smoke explicitly sent `model_id=eleven_v3` with `[laughs]` and `[sighs]`; AI33 returned `task_id=08c146ad-82a0-4efb-a4e2-f8ec65254852` and wrote `/tmp/reddit_ai33_eleven_v3_laugh.mp3`.
- `afinfo` verified the explicit `eleven_v3` smoke output as a 5.64s MP3, 44.1 kHz stereo, 128 kbps, 90,740 bytes.
- AI33 v3 task polling uses `/v3/task/{task_id}` with `Authorization: $AI33_API_KEY` by default. If account-specific routing differs, set `AI33_TASK_URL_TEMPLATE` or `AI33_TASK_AUTH_HEADER` before running TTS.

## Gemini / VectorEngine State

- Text Gemini calls now prefer direct Google Gemini through `GOOGLE_GEMINI_API_KEY`, `GEMINI_API_KEY`, or `GOOGLE_API_KEY`. The client uses `x-goog-api-key` against `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`, default `model=gemini-3.5-flash`.
- GitHub Secrets readback on 2026-07-04 confirmed `GOOGLE_GEMINI_API_KEY` is present, updated at `2026-07-04T05:59:44Z`. Once the Google-first routing changes are pushed, cloud workflows should use direct Google Gemini for text calls and keep `VECTORENGINE_API_KEY` as fallback/image provider.
- If no Google Gemini key is present, `vectorengine_client.py` falls back to the LUNA2-compatible VectorEngine env names `VECTORENGINE_API_KEY`, `VECTOR_ENGINE_API_KEY`, and optional `VECTORENGINE_BASE_URL`.
- Set `GEMINI_PROVIDER=google` or `GEMINI_PROVIDER=vectorengine` only when intentionally forcing a provider. In normal CI/local use, leave it unset so Google Gemini is used when configured and VectorEngine remains a fallback.
- `vectorengine_client.py` retries Gemini JSON calls on transient request/API/empty-text failures; override with `GEMINI_RETRIES` or legacy `VECTORENGINE_GEMINI_RETRIES` if needed. This protects publish runs from one-off empty-text responses during translation or metadata generation.
- In `GEMINI_PROVIDER=auto` mode, `vectorengine_client.py` now falls back to VectorEngine Gemini immediately when Google returns HTTP 429 and a VectorEngine key is configured, instead of waiting through Google retry delays. This keeps live dry-runs from stalling on Google free-tier request bursts while preserving explicit `GEMINI_PROVIDER=google` as Google-only.
- `thumbnail_generator.py` uses the LUNA2-compatible image endpoint shape: `POST /v1/images/generations`, default `model=gpt-image-2`, size `1536x864`.
- Live metadata smoke passed on 2026-06-29 using the gitignored LUNA2 VectorEngine env file as a one-off user-approved read. The secret was not printed or copied into this repo.
- Smoke command wrote `/tmp/reddit_vectorengine_metadata_live.json` with `source=vectorengine-gemini`, `model=gemini-3.5-flash`, `channelId=acc4`, `language=es-419`, 79-character Spanish title, 323-character description, 8 tags, 4 hashtags, thumbnail text, thumbnail prompt, SEO keywords, and no risk flags.
- Thumbnail image generation has only been dry-run checked in this repo. It is connected but not live image-spend verified.

## Current Pipeline Changes

- `translator_tts.py` now translates `title`, `body`, and each comment `body` for non-English channels before TTS. By default it overwrites `story_data.json` so downstream storyboard/render steps see localized text; use `--translated-story-output` to write a separate localized file.
- The default narration order mirrors visible card text: title, body, then comment bodies. For raw URLs and service link lines, visible fields keep the actual link on the card while `narration_title`, `narration_body`, and `comments[].narration_body` say the localized "link on screen" phrase. For Russian narration, visible numeric tokens stay unchanged in display fields while the narration-only fields spell numbers out for TTS, for example `6500+` -> `более чем шесть тысяч пятьсот` and `100%` -> `сто процентов`. `--include-comment-labels` can restore spoken "Comment by user" labels when desired.
- If `comment_tts_voice` differs from `tts_voice`, `translator_tts.py` now runs multi-voice TTS: title/body use the narrator voice, comment bodies use the comment voice, long text is chunked with `--tts-segment-max-chars`, and FFmpeg concatenates the MP3 segments. Current workflows request `--with-transcript`; transcript parsing/timing data is used for static slide boundaries while visual karaoke remains off. AI33 subtitle outputs (`subtitle`, `srt`, `vtt`, `subtitle_url`, `srt_url`, `vtt_url`, and base64 equivalents) are parsed into word-like cue timings before falling back to estimated timings. Retryable AI33 task errors during per-segment multi-voice generation are retried before the pipeline fails.
- `translator_tts.py --check-voice-config --require-voice-prefix elevenlabs_` validates narrator/comment voice ids without reading story text or calling AI33. `auto_publish.yml` runs this preflight before Reddit/Gemini/AI33 work.
- `translator_tts.py` now handles raw URLs and service link lines such as `Original thread: https://...` as narration-only substitutions. Storyboard/display fields keep the visible URL, while TTS reads localized phrases such as "el enlace está en pantalla" so videos show the link instead of printing the spoken placeholder.
- `storyboard_generator.py`, `app.js`, and `style.css` now render videos as centered slides rather than one long scrolling post. Story slides never include comments; comment slides show only comments; long story text is chunked into additional story slides. Render-mode typography now uses a Reddit-like system UI font stack, larger Shorts/16:9 text, wider cards, tuned slide character limits, and a subtle dark grain/vignette background so the card uses more of the frame without decorative clutter.
- `render.py` now auto-detects default `narration.mp3`, captures deterministic clean timed-slide frames by default, and merges the audio track into `final_output.mp4`. Current workflows pass `--no-karaoke` to disable visual highlights, but still generate and read `narration.json` when available. Render orientation is duration-aware: `<=180s` stays vertical 9:16, `>180s` becomes horizontal 16:9.
- Acc1 unlisted live run `28573148388` on 2026-07-02 succeeded through upload to `gZP4fiM5iTo` on `ChonkerTalks Россия` with `privacy=unlisted` and `language=ru`, but AI33 returned transcript metadata without complete word timings for a very long narrator segment, so `render.py` fell back to non-karaoke slide-progress before upload. Follow-up runs `28574532627` and `28576525232` stopped before upload because `auto_publish.yml` requires karaoke and AI33 task payloads still did not expose usable timings, even for a short 4-segment / 1718-character story. The follow-up fix now chunks long TTS narration, parses ElevenLabs character alignment when available, and uses estimated per-word timings from text plus segment audio duration when AI33 returns audio without timings. Acc1 unlisted run `28693621091` on 2026-07-04 verified that this path can pass `--require-karaoke` and upload with in-text karaoke enabled.
- `style.css` now hides editor/sidebar/HUD/desktop-nav/sidebar/safe-zone widgets under `.clean-mode` and `.render-mode`, including desktop layout states. The 16:9 render layout uses a wide clean Reddit card with horizontal-specific spacing so long-form output is not a vertical canvas with empty side filler. Render-mode background is now a restrained dark Reddit-like surface with subtle grain and vignette, not a bright decorative gradient. Local no-spend smokes verified clean vertical static slides, vertical fallback render with bad transcript, vertical slide karaoke render with gold word highlight, and horizontal desktop slide karaoke render with no control/sidebar/topbar widgets and AAC audio in the MP4.
- `.github/workflows/auto_publish.yml` and `.github/workflows/video_dry_run.yml` now pass `GOOGLE_GEMINI_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `VECTORENGINE_API_KEY`, and `AI33_API_KEY` to the relevant text/TTS steps. Google Gemini is preferred for text calls when the secret is configured; VectorEngine remains fallback and image provider.
- `.github/workflows/audit_voice_youtube.yml` is a manual audit workflow for two isolated checks: short AI33 voice samples through `scripts/generate_ai33_voice_samples.py`, and YouTube refresh-token mapping via `uploader.py --check-channel-only` for `acc1` through `acc7`. It does not fetch Reddit, call Gemini/VectorEngine, render video, or upload to YouTube. Its voice-sample path exposes `text_style`, `voice_settings_profile`, and an optional raw `voice_settings_json` override; start listening tests with `text_style=emotional` and `voice_settings_profile=creative`, then compare `natural` if the voice is too theatrical. Manual `auto_publish.yml` and `video_dry_run.yml` also expose `voice_settings_profile`, but scheduled runs remain on `default`.
- `scripts/issue_youtube_refresh_token.py` is the local helper for reissuing scoped YouTube refresh tokens. It runs a local OAuth consent flow for one `acc#`, requests `youtube.upload`, `youtube.readonly`, and `youtube.force-ssl` by default, and can store the resulting token directly into `YOUTUBE_REFRESH_TOKEN_ACC#` with `gh secret set` without printing the token.
- Audit run `28457170166` on 2026-06-30 generated all 14 AI33 voice samples successfully and uploaded the `ai33-voice-samples` artifact. The local review page is `build/audit/run_28457170166/ai33-voice-samples/20260630T154616Z/voice_samples_review.html`.
- Historical mapping-only audit run `28458168727` saved per-account logs under `build/audit/run_28458168727/youtube-mapping-acc*/acc*.log`. That pre-reissue run reached YouTube but failed before channel comparison with Google `403 insufficient authentication scopes`. Per current user-provided state, the seven tokens/scopes were reissued afterward and are no longer the active blocker.
- Historical scope-aware audit run `28459324708` confirmed the exact granted scope for the pre-reissue `YOUTUBE_REFRESH_TOKEN_ACC1-7` values: only `https://www.googleapis.com/auth/youtube.upload`. That explained why adding scopes in Google Cloud/OAuth configuration did not update the refresh tokens stored in GitHub Secrets. Per current user-provided state, the seven tokens/scopes were reissued after that audit.
- `scraper.py` now supports `--time auto`, `--topic-family`, `--max-ai-candidates`, `--candidate-limit`, `--similarity-threshold`, and `--producer-queue-output`. `auto` mode scans capped topic-family source plans rather than only `top/week`, applies channel `topic_exclusions`, then sends only the top bounded pool to Gemini. The topic-search prompt has been upgraded from a loose fit check to an outside-in producer gate: Reddit upvotes are evidence only, and Gemini must skip generic Reddit filler, screenshot/link-dependent posts, narrow-fandom posts, topics that cannot carry the requested format, and candidates likely to look like mass-produced AI content. Approved stories now carry packaging fields such as `content_bet`, `audience_job_fit`, `first_screen_promise`, `first_screen_text`, `packaging_thesis`, `why_now`, `shorts_cut`, and `longform_angle`; each candidate also needs exact source-backed `hook_evidence`. The scraper writes `producer_queue.json` and selects the best approved candidate by producer score instead of stopping at the first pass.
- `story_adapter.py` now runs in `auto_publish.yml` and `video_dry_run.yml` after scraper selection. It performs a source-backed no-invent cleanup, writes `editorial_adaptation` into `story_data.json`, and fails strict runs when the hook evidence quote cannot be found in source text. This is the boundary between raw Reddit source material and narrated script text.
- `metadata_generator.py` now generates three packaging options and stores them in `youtube_metadata.json.packaging_options` with a selected option index. The uploaded metadata remains backward-compatible through `youtube_title`, `thumbnail_text`, tags, hashtags, and SEO keywords.
- `scraper.py` also supports format-specific source-length selection through `--format-intent`. Production `shorts` runs now select complete short source stories before adaptation instead of cutting long posts after selection; current source-body policy is roughly `250-2400` chars for Shorts and `2800+` chars for long-form. The Gemini producer gate receives the full short-source body up to the Shorts limit, so it should not reject complete short candidates as "truncated" merely because the old prompt preview ended early. Scraper/adapter `--max-body-chars` is now a deprecated manual safety valve and is ignored unless `--allow-body-trim` is passed.
- `published_history.json` remains backward-compatible with the old `{post_id: [channels]}` shape; the next scraper save migrates future entries to versioned records with story signatures, keyword signatures, topic family, time window, velocity, fatigue penalty, virality score, and AI quality data.
- `.github/workflows/auto_publish.yml` supports manual topic strategy and content length selection and uses `privacy_status=unlisted` by default for manual dispatch. `content_format=auto` resolves to `long` for `acc1` and `shorts` for the other channels; scheduled `acc1` entries explicitly use `long`. Long selection preserves the full source and lets render auto-switch to 16:9 when audio exceeds 180 seconds. Explicit `shorts` for `acc1` uses the isolated source-backed trailer adapter. `video_dry_run.yml` follows the same format routing. Its live producer dry-run budget is capped at 12 Gemini candidates, metadata can use a marked fallback for artifact inspection, and both workflows remain fail-closed through adaptation, TTS/model, render, and pre-publish QA gates.
- `uploader.py` now supports `--check-channel-only`, `--privacy-status public|unlisted|private`, merges `tags` + `seo_keywords`, appends returned `hashtags` to the description, passes metadata language to YouTube, verifies the authenticated YouTube channel before upload, and reads back uploaded video metadata afterward. `auto_publish.yml` runs the channel check before any content-generation spend. Manual and scheduled runs should stay `unlisted` until one post-fix artifact is inspected end to end.

## Known Blockers

- No channel has retention/viewer proof for its new promise. Exact `topic_mix` weights remain unapproved; the 2026-07-11 committed-config reviews disproved the present mixes as publish selectors but did not establish replacement weights.
- AI33 A/B run `29153420437` generated `acc1` narrator/comment samples with `model_id=eleven_v3`, emotional text, and provider-default voice settings. Run `29153423227` used the same voices/text/model with creative settings `stability=0.35`, `similarity_boost=0.75`, `style=0`. Both runs succeeded; their YouTube mapping jobs were skipped, and no Reddit, Gemini, render, or YouTube upload path ran.
- The four downloaded MP3s are valid 44.1 kHz stereo, approximately 128 kbps. Default durations were 11.154s narrator / 5.799s comment; creative durations were 10.684s narrator / 6.034s comment. Manifests prove the requested `eleven_v3` contract but do not include a separate provider-echoed resolved model, so the honest verification level is requested-model plus successful synthesis, not independent server echo.
- The strategy reset, held-channel guard, network-wide dedupe, tests, and source-review workflow are pushed only on branch `codex/reddit-topic-source-review-20260711` at `ca6b0ad`. `origin/main` remains unchanged and does not enforce the new holds.
- Source-only runs `29073999465` through `29074038795` used commit `010fb95`, not the local revised `channels.json`. They therefore validate Reddit access and old-family source behavior only. Across 12 queues, only eight unique candidates appeared and four unique stories were selected; generic AITA repeated across `acc1/acc4/acc5/acc7`, while source-incomplete OutOfTheLoop questions repeated across `acc3/acc4/acc6`.
- Committed-config runs `29152777746` through `29152780145` used `ca6b0ad`, config SHA-256 `588466d4e6250328660a17f19fa8cb84462f82ccbca7934125d10b39184f56e1`, `AI_QUALITY_CHECK=0`, separate `week`/`month` windows, one subreddit per family, ten posts maximum per source, no history write, and no Gemini/AI33/render/YouTube. They produced 63 queue entries but only 19 unique posts; 28 entries were link-dependent and the same two posts won all 12 runs.
- `acc2` was not included in those reviews and still needs a dedicated high-competition competitor/source-lane review.
- The `evidence_dossier` source/script/render lane does not yet exist. This blocks `acc2`, `acc3`, `acc5`, and `acc6`, plus any factual case on another channel.
- The current Reddit-card output still needs a visibly original producer take and fiction/claim disclosure contract before it can be treated as safe from inauthentic-content/reused-reading risk.
- `acc1` has no permission/license ledger or distribution rights gate. Public Reddit text is user-owned; attribution and Reddit upvotes do not grant commercial adaptation rights. The two internal artifact-only tests are the sole temporary exception for paid adaptation/TTS/render and remain fail-closed against upload; any distribution remains blocked until the rights gate exists.
- Scheduled runs `29070128277` and `29072898300` were verified no-ops: each completed only `resolve`, while `publish` was skipped. The scheduler derives routing from the runner's actual UTC hour, so delayed GitHub cron starts can miss the intended matrix hour. This is an operations blocker, not a content success.
- GitHub Reddit source access itself is healthy: isolated source-only run `29069048314` and all twelve 2026-07-10 review runs passed credential preflight, bounded PRAW fetch, artifact upload, and result enforcement. The local terminal still needs explicit `REDDIT_*` environment values.
- Direct Google Gemini text routing is implemented and verified in dry-run `28703717316`: metadata and localization artifacts reported `source=google-gemini`. The key that was pasted into chat should still be treated as exposed and rotated before production use.
- `scripts/move-to-trash.sh` is the project safe-trash helper; generated scratch artifacts should be moved through that workflow, not direct deletion commands.
 
## Next Steps
 
1. Review branch `codex/reddit-topic-source-review-20260711`; do not merge it as permission to enable publishing or spend.
2. Stop additional `acc1` Reddit popularity reviews: premise supply is already proven. Build its rights manifest, full-story reviewer, Russian episode-script artifact, independent editorial checkpoints, and scene-based storyboard before any paid pilot.
3. Keep all numeric topic weights unapproved until audience pilots produce retention evidence; source weights cannot substitute for rights or editorial readiness.
4. Build one deterministic `evidence_dossier` fixture with fact/claim/theory separation and timeline/evidence visuals before testing `acc2`, `acc3`, `acc5`, or `acc6`.
5. Fix scheduled routing so delayed cron starts map from `github.event.schedule` rather than the runner's current UTC hour, while keeping all channels fail-closed.
6. After source gates pass, run small manual/unlisted pilots and collect `Engaged views`, `Stayed to watch`, average percentage viewed, shares/comments/subscribers per 1,000 engaged views, and returning-viewer readback. Change numeric topic weights only from repeated winners.

## DESIGN.md Agent Bridge

Installed 2026-07-08: project-root `DESIGN.md` is now available as an agent-facing visual/design brief using the plain Markdown DESIGN.md pattern from https://github.com/VoltAgent/awesome-design-md. It is a bridge for UI/visual work and does not supersede the existing source-of-truth docs listed in `docs/README.md`.
