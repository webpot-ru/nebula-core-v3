# nebula-core-v3 Project State

Last updated: 2026-07-17

## Current Shape

`/Users/lali/Projects/reddit` is a multilingual YouTube story-entertainment pipeline for the ChonkerTalks channel network.

- PR [#44](https://github.com/webpot-ru/nebula-core-v3/pull/44) merged the acc1 cinematic/release implementation to `main` as `017934e3c96be5d9df6eb26698cfb3420a856d05`. `reddit_pages` remains the explicit default; opt-in `cinematic_story_v1` supports SAGA/BUNDLE with checksum-bound full-screen slow zoom/pan, captions, five pillar-aware narration profiles, semantic chunks, pause map, measured voice-only mix, manifest v2 and mode-aware QA. THREAD fails closed pending a distinct response-card contract. The targeted verification is 224/224 tests plus a 7/7 workflow-specific recheck and the real no-network fixture PASS. Persistent A/B evidence is `/tmp/acc1-cinematic-comparison-main-20260717`: both 1920×1080 H.264/AAC 30 fps MP4s pass media QA, share final audio SHA `0db0f81f8d8665a8abf0a1eab512b959114d636ea0fedef89d98418deb7dfd05`, and have comparison SHA `af329ebd0bd2818ab8df431faa19be73b84695017fc3ee18d7c1055592ed15e8`. Synthetic fixture visuals/tones keep the creative verdict `BLOCKED_PENDING_HUMAN`.
- The factory-aware post-human release chain is merged and registered. `acc1_rights_manifest.py` requires exact private-scope commercial/translation/adaptation/narration/audiovisual permission evidence for every source without storing agreement text. `acc1_release_gate.py` requires the exact factory artifact, completed checksum-bound human review, media QA and rights evidence and can return only `READY_FOR_PRIVATE_REVIEW`, with upload/publication flags false. Active `acc1 Release Review Gate` workflow `314883599` has no provider or YouTube access. Active hardened `acc1 Private Artifact Upload` workflow `313326356` requires that exact gate run/hash before a separately confirmed one-video private upload; it has not been dispatched.
- Authorized artifact-only cinematic canary [run `29555487790`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29555487790) ran on `main@017934e3` with bounded caps `Reddit 24 / OpenAI 32 calls, 250000 tokens / image 8 / AI33 32`. It stopped in deterministic source review before the spend lease and before every paid provider: four complete runtime-fit SAGA sources were collected, but only one passed because a harmless final Reddit author-profile link was classified as a story dependency. Replaying the saved queue with the corrected classifier yields two eligible sources (`1uvso1w`, `1me740h`) while genuine part-2/continuation links remain blocked. The same correction raises SAGA/BUNDLE discovery from 25 to 50 posts per subreddit/window under the unchanged HTTP request cap, so the factory can seek the required 3-5 finalists without weakening runtime, payoff, safety, rights, review or release gates. Artifacts are under `/tmp/acc1-cinematic-canary-29555487790/`. No OpenAI, image, AI33, render or YouTube action occurred in this run.

Current content strategy: the old multilingual translated-Reddit pool has been replaced locally by explicit channel ownership. The full source of truth is [`topic-strategy-research-2026-07-10.md`](topic-strategy-research-2026-07-10.md).

- Each channel now has one unique `viewer_promise`, owned content bets, forbidden bets, an evidence policy, a render contract, and a gated cadence in `channels.json`.
- `acc1` now owns Russian Reddit-story entertainment across five pillars: relationships/family, work/money/justice, confessions/awkward/taboo, professions/human experience, and strange/dark/unexplained. Horror remains one series rather than the whole channel. `acc2` owns high-concept internet case files; `acc3` digital-system explainers; `acc4` LATAM moral court; `acc5` human football stories; `acc6` skeptical web-mystery dossiers; `acc7` Italian social absurdity.
- `acc1` now has three daily pilot formats: `BUNDLE` (2-3 relationships/family stories or 3-5 work/justice stories), `SAGA` (one complete strange/dark/unexplained story), and `THREAD` (one prompt plus 8-15 complete responses). The existing 45-60 minute `reddit_horror_compilation` code remains an isolated dark-series artifact lane instead of being generalized or removed.
- The canonical current acc1 decision is [`acc1-russian-reddit-story-strategy-2026-07-13.md`](acc1-russian-reddit-story-strategy-2026-07-13.md). The local six-slot cycle is `pilot_01, pilot_04, pilot_02, pilot_05, pilot_03, pilot_06`; it is derived from exact pilot rows and never from the superseded numeric `topic_mix`. `acc1_bundle_selector.py`, `acc1_thread_collector.py`, and `acc1_thread_source.py` implement the bounded complete-source contracts. THREAD has live source plus paid-review transport proof, and BUNDLE now has a clean post-fix GitHub source-stage proof. Neither format yet has a complete review-ready episode artifact.
- Deployment state, verified 2026-07-15: PR [#3](https://github.com/webpot-ru/nebula-core-v3/pull/3) is merged as `f47cc0d`; PR [#5](https://github.com/webpot-ru/nebula-core-v3/pull/5) as `5e8c00e7342fc494b79c3cc9bc720ca160421ad0`; PR [#6](https://github.com/webpot-ru/nebula-core-v3/pull/6) as `bc213ac2ea2eac6670148220c12dc76f435ff440`; diagnostic-order PR [#7](https://github.com/webpot-ru/nebula-core-v3/pull/7) as `b6386f9b0f586ac6fc2beaa7603245f3cd1bfa8a`; and BUNDLE-window PR [#8](https://github.com/webpot-ru/nebula-core-v3/pull/8) as `afd67946d76347fd4821f64f9a42bdc5c621f246`. The active factory routes producer, independent critic, bounded evidence-only correction, translation, translation review, and packaging through one pinned `gpt-5.4-2026-03-05` Flex budget and removes Gemini from this workflow. It binds one OpenAI call/token cap, one attempt journal, exact returned `service_tier=flex`, and spend-lease schema v5. After title-repair and durable pending-review accounting, the current three-finalist THREAD proof requires cap 100 and the maximum THREAD envelope requires cap 128; 500,000 is the combined input-plus-output token ceiling. Live canary `29415356438` verified six Flex responses before the editorial playoff blocked. The factory remains artifact-only and stops at `READY_FOR_HUMAN_REVIEW`; it neither guarantees views nor invokes an uploader.
- Source-only GitHub run [`29388486415`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29388486415) on merged commit `5e8c00e` completed with failure before every paid-provider step. Reddit access worked for `pilot_04 / THREAD / confessions_awkward_taboo`, but the old collector rejected every bounded AskReddit prompt: several had fewer than eight eligible full responses, while three had enough source responses but failed the greedy 1,950-word / three-role / 40%-role-share selector. No Gemini, OpenAI, image, AI33, render, upload, or YouTube action occurred.
- Follow-up source-only branch run [`29412649632`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29412649632) on `aea8eed` live-verified the replacement THREAD selector: the same bounded `pilot_04` route advanced from zero valid prompts to two complete production-fit prompt/response sets. It then failed closed at the independent topic-playoff gate, which requires 3-5 source candidates before paid review; all paid/provider/render/upload steps were skipped. The workflow preserved only plan/preflight artifacts because the insufficient-pool branch did not yet persist source diagnostics.
- The next source-only branch run [`29412959997`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29412959997) on `13b0b0d` expanded discovery to 20 prompts under the unchanged cap `24` and advanced beyond the 3-5-candidate gate, proving that the wider bounded pool supplied enough production-fit THREAD candidates. It then falsely blocked while reading the request count: the code expected the legacy `reddit._core._requestor`, while installed PRAW 8 exposes the bounded requestor as `reddit._core.requestor`. The requestor itself still enforced cap 24 before every HTTP call. All paid/provider/render/upload steps were skipped.
- Final source-only branch run [`29413265489`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29413265489) on `b1ea226` live-verified the complete fix. `pilot_04 / THREAD / confessions_awkward_taboo` reached `SOURCE_READY` with exactly 3 complete candidates and 22/24 observed Reddit HTTP requests, then stopped at the intentionally false paid-provider confirmation gate. Gemini, OpenAI, image, AI33, lease, production, render, upload, and YouTube steps were skipped. Downloaded evidence is under `/tmp/acc1-source-only-29413265489/`: source-stage, candidate-pool, queue, review, and all three THREAD manifest hashes verify; all three sets are diverse, self-contained, runtime-fit, and explicitly `unverified_personal_account`. Their deterministic source-text interest averages are `38.58`, `65.62`, and `44.60`; these are discovery heuristics, not audience-performance evidence. Candidate 2 is the current source-signal leader, but all three pools contain sensitive themes and unverified allegations, so none is greenlit before the separate producer/critic and human review gates.
- Paid artifact-only canary [`29415356438`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29415356438) on merged PR #6 commit `bc213ac` verified the unified GPT-5.4 Flex transport and fail-closed topic gate. Source collection, paid preflight, and spend-lease schema v5 passed for `pilot_04 / THREAD`; six producer/critic calls completed with returned `service_tier=flex`, using 29,219 input plus 5,608 output tokens (34,827 total, zero cached input) under caps 96 / 500,000. The playoff then blocked because fewer than three finalists independently passed. Image and AI33 made zero calls; no render, upload, or YouTube step ran. Artifacts are downloaded under `/tmp/acc1-canary-29415356438/`. The run exposed a diagnostic-order defect: producer/critic and playoff payloads were written only after a successful playoff, so this run preserves hashes and aggregate usage but not the exact model rejection reasons. The follow-up local fix writes all four review/playoff artifacts before the fail-closed gate and is verified by 380/380 tests; no retry is authorized or attempted.
- Next-date canary [`29416438184`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29416438184) on `b6386f9` selected `pilot_02 / BUNDLE / work_money_justice` and stopped in the Reddit source stage before any spend lease or paid request. The bounded `day/week/month` read produced 19 full queue entries, but deterministic review retained only five complete/pillar-fit/payoff-complete components; their combined length produced exactly one valid 2,340-3,900-word subset, so three materially distinct finalist bundles were impossible. OpenAI, image, AI33, render, upload, and YouTube made zero calls. Evidence is under `/tmp/acc1-canary-29416438184/`. Because day supplied no eligible long component and month supplied most usable sources, the correction keeps the same three-window/request envelope but routes BUNDLE discovery through `week/month/year`. It also persists exact BUNDLE selector diagnostics before raising. Completeness, payoff, pillar, runtime, truth, and S-tier gates are unchanged. The window correction was verified by 382/382 tests and the following Reddit-only run; that run then exposed the separate boolean-mapping defect.
- Reddit-only proof [`29417105629`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29417105629) on merged commit `afd67946` live-verified the BUNDLE `week/month/year` routing under request cap 24 with all paid confirmations false. It collected 62 complete queue entries, but the source stage blocked before paid confirmation because `_story_source()` inverted the already-reviewed `depends_on_screenshot_or_link=false` value to `true`. The saved queue itself records `source_has_url=false`, `source_has_markdown_link=false`, `source_has_markdown_image=false`, and empty `source_media` for the selected components, proving this was a contract-mapping defect rather than media-dependent source text. Replaying that exact queue locally after the fix yields five valid finalist bundles of five stories each, 3,054-3,118 aggregate words, with every dependency flag false. OpenAI, image, AI33, render, upload, and YouTube made zero calls; artifacts are under `/tmp/acc1-source-proof-29417105629/`. The mapping now preserves explicit false and treats missing/unknown values as dependent (fail-closed); base-contract failures also persist self-hashed `source-diagnostics.json`.
- Clean post-fix source proof [`29418091062`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29418091062) on merged commit `c4a2c3749235008ef6223bea8af1ba28a67c53a6` reached `SOURCE_READY` for `pilot_02 / BUNDLE / work_money_justice`, then stopped exactly at the intentionally false paid-provider confirmation gate. The bounded `week/month/year` read used 13/24 Reddit HTTP requests, produced 62 full queue entries and 20 eligible deterministic components, and selected five materially distinct finalist bundles of five stories each. Their aggregate source lengths are 3,003-3,116 words; all 25 source slots are self-contained, `unverified_personal_account`, and have `depends_on_screenshot_or_link=false`. Daily-plan, queue, review, candidate-pool, finalist-manifest, and source-stage hashes/bindings all verify. OpenAI, image, AI33, spend lease, production, render, upload, and YouTube did not run. Evidence is under `/tmp/acc1-source-proof-29418091062/`. This proves source supply and deterministic BUNDLE routing, not factual truth, editorial S-tier quality, retention, or audience performance.
- Paid artifact-only canary [`29418586274`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29418586274) on merged commit `2924d1b13d36f8fb55494c9f013ea89d95ec19e1` passed source collection, overlap scan, source-dependent paid preflight, and durable spend lease, then blocked at the independent S-tier topic playoff. Ten GPT-5.4 producer/critic calls all returned `service_tier=flex`, using 59,021 input plus 9,347 output tokens (68,368 total, zero cached input) under caps 48 / 500,000. Image and AI33 journals contain zero attempts; no translation, packaging, render, upload, or YouTube step ran. Evidence is under `/tmp/acc1-canary-29418586274/`. The artifacts prove two separate causes: four of five old length-ranked bundles received genuine viewer-promise/payoff vetoes, while both producer and critic prompts left the weighted score ranges unspecified, so most responses used invalid 0-100 category values. Candidate 4 received the only producer PASS but the critic also falsely treated provenance as screenshot/link dependence. The local correction does not weaken any S-tier threshold: it ranks already-valid subsets by minimum and aggregate deterministic shortlist quality before runtime proximity, records those quality scores in each finalist, gives both reviewers exact weighted maxima/minima and canonical veto names, and states that a canonical Reddit URL is provenance rather than dependency. Replaying the exact live pool changes finalist minimum shortlist scores from roughly 59-67 to 106-109 while remaining inside 2,518-3,184 source words. This is a deterministic selection improvement, not proof that a future paid review will pass.
- Follow-up paid canary [`29423078460`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29423078460) on merged commit `79033a651848aa018b40c486fb8d135676a3c9a8` tested the corrected selector/prompts on a fresh `pilot_01 / BUNDLE / relationships_family` episode. Source, overlap, paid preflight, and durable lease passed. Ten GPT-5.4 Flex reviews used 51,848 input plus 9,669 output tokens (61,517 total, zero cached); image and AI33 made zero attempts, and no downstream media or YouTube step ran. All returned scorecards obeyed the exact weighted ranges, proving the prompt defect fixed. One candidate cleanly passed both independent reviews and every evidence/packaging/hard gate at `96.5`; the other four were honestly blocked for viewer mismatch, wrong pillar, advertiser safety, or truth/packaging honesty. The run nevertheless blocked because the old pool-level rule required three independently passing episodes. The local correction keeps that normal three-PASS rule and adds only a narrow exceptional-winner path: after five distinct candidates are reviewed, one winner may advance alone only with both reviews PASS, every category/hard gate satisfied, zero failures, and score >=95. The exact canary checkpoint re-evaluates to `READY_FOR_SCRIPTING` through this exceptional path without changing any winner artifact. Automatic paid resume remains disabled and the prior lease is not reused.
- Fresh paid artifact-only canary [`29424234813`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29424234813) on merged commit `426b970229f3204dd42f7cee30f4eb66bddc03f7` tested `pilot_03 / SAGA / strange_dark_unexplained`. Source collection found 12 complete candidates and retained three runtime/payoff-eligible `r/nosleep` stories; overlap, paid preflight, and the durable lease passed. Six GPT-5.4 Flex reviews used 34,805 input plus 5,836 output tokens (40,641 total, zero cached). The playoff blocked all three before translation: image and AI33 journals contain zero attempts, and no MP4 or YouTube action exists. Evidence is under `/tmp/acc1-canary-29424234813/`. The exact reviews exposed a prompt-contract contradiction rather than a source-truth failure: every source was already classified `truth_mode=fiction`, while the producer/critic treated fiction itself as `fictional_as_real` and misread the viewer promise's "без выдуманных продолжений" as excluding fiction. The canonical strategy explicitly allows `r/nosleep` with the mandatory audible and metadata disclosure `Это художественная история с Reddit.` The local fix states that distinction in both review prompts and preserves the hard veto only for proposals that explicitly present fiction as verified/real or suppress the disclosure. It changes no score floor, source gate, topic mix, or publication hold and passes 390/390 local tests. A future paid run is required to live-verify the corrected semantic behavior; no automatic retry is authorized.
- Post-fix canary [`29425211315`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29425211315) on merged commit `f0c0bac8400e274d999de855d387d80abc6cee8a` used the separate `2026-07-24 / pilot_03` episode key. Plan, preflight, Reddit source collection, and artifact upload passed, but the cross-date reservation gate correctly blocked the exact same three source IDs already protected by run `29424234813`. It stopped before the paid confirmation/lease/production steps, so OpenAI, image, AI33, render, and YouTube made zero calls. Evidence is under `/tmp/acc1-canary-29425211315/`. This exposed a source-rotation ordering defect: validated prior leases were downloaded before Reddit collection but their reserved sources were only compared after the selector had chosen them again. The local correction writes a self-hashed exclusion manifest from validated leases during the pre-source scan, passes prior source IDs and story signatures into SAGA/BUNDLE discovery and prior prompt IDs into THREAD discovery, and retains the existing post-source overlap scan as a second fail-closed guard. It changes no quality threshold or publication setting and passes 390/390 local tests. A fresh no-spend/source or paid canary is still required to prove whether the bounded pilot 03 pool contains three non-reserved eligible alternatives; no retry has been launched.
- Rotation-proof canary [`29426034901`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29426034901) on merged commit `c704fc242cb87b4dbcad3251a54fd247309f36c6` verified that prior reservations are excluded before ranking. It found nine new full-body `r/nosleep` stories in the 2,340-3,900-word SAGA envelope and did not reselect any of the three reserved posts, then failed closed at deterministic review before paid confirmation. OpenAI, image, AI33, render, and YouTube made zero calls; evidence is under `/tmp/acc1-canary-29426034901/`. Local replay of the exact queue showed that all nine were rejected only because `_payoff_evidence()` required a narrow connective phrase such as `finally` or `in the end`; six had no screenshot/link dependency and five otherwise satisfied the exact runtime/pillar/source contract. The correction still blocks explicit series markers, explicit open-ending language, and non-terminal text, but accepts a punctuated terminal ending with no open-ending marker for independent semantic payoff review by the producer and critic. Replaying the exact live queue now yields five non-reserved finalists; the two deterministic leaders score 98 and the remaining three 54/50/46. These are source heuristics, not audience or S-tier proof. The correction passes 391/391 local tests; no further live run is authorized or attempted.
- Paid payoff-proof canary [`29427525245`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29427525245) on merged commit `2960d6c4cc8ed87a636a1b7473e1f0541ab2752c` advanced the five new non-reserved SAGA finalists through source, overlap, paid preflight, lease, and ten GPT-5.4 Flex producer/critic calls. Usage was 60,631 input plus 10,477 output tokens (71,108 total, zero cached); image and AI33 made zero attempts, and no translation, render, or YouTube step ran. Evidence is under `/tmp/acc1-canary-29427525245/`. Three candidates independently received `PASS/PASS` at 94/94, 94/90, and 91/91, which would satisfy the normal three-finalist policy, but the producer had paraphrased or underspecified one or more exact evidence quotes, so source-truth validation correctly blocked them. Two other candidates were independently blocked for missing payoff or advertiser-hostile treatment. The correction adds at most one planned evidence-only GPT call per finalist and only when both reviews already PASS and every remaining failure is a quote-path failure. It may replace only the named `source_quote` with an exact >=24-character, >=4-word substring from the unchanged source_id; it cannot change the hook, scores, vetoes, packaging claim, plot, or review. The preliminary and final playoffs plus repair report are preserved separately, invalid repair output remains blocked, and the exact five-SAGA pool reserves 24 OpenAI calls under cap 48.
- Resume registration run [`29430344440`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29430344440) stopped in the dependency-free plan test because the new lock test imported the Pillow-dependent factory before dependency installation. Build never started, no resume lock existed, and OpenAI/image/AI33/Reddit/YouTube made zero calls. PR #18 moved the journal-continuation test into the full factory suite; exact plan tests then passed 37/37.
- Hash-bound continuation [`29430564438`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29430564438) restored run `29427525245`, skipped every Reddit/source step, validated and uploaded the single-use resume lock, and continued the OpenAI journal at call 11. Three evidence-only calls completed, increasing usage from 71,108 to 84,220 total tokens (73,388 input plus 10,832 output, zero cached). All three quote-only finalists were corrected; final playoff is `READY_FOR_SCRIPTING` with winner `saga-1q5rhvs`. It then stopped before translation/images/AI33 because the manifest's secret-key detector treated the legitimate field `max_output_tokens` as a secret. Image attempts remain zero, AI33 was not initialized, and no render or YouTube step ran. Evidence is under `/tmp/acc1-resume-29430564438/`. The local correction matches secret names/suffixes without rejecting plural token-accounting fields, and a chained resume lock can bind this exact child plus its prior resume lease while preserving all 13 completed calls. Exact chain validation passes on the downloaded artifact; 398/398 local tests pass. Another paid continuation still requires separate explicit approval.
- Approved chained continuation [`29431174782`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29431174782) restored all 13 completed calls from `29430564438`, skipped Reddit, validated the prior resume ancestry, and passed playoff plus immutable episode-plan creation. Five additional GPT-5.4 Flex calls completed, so the preserved journal now contains 18/48 `COMPLETE` attempts and 114,230/500,000 tokens (90,943 input plus 23,287 output, zero cached). The full-story translation triggered chunk fallback after the narration gate reported `contextual_year`; the glossary then returned a 151-character English analysis instead of a Russian title, while all three saved Russian body chunks are complete. The run stopped before images/AI33/render; image attempts are zero, AI33 was not initialized, and YouTube is absent. Evidence is under `/tmp/acc1-resume-29431174782/`. Exact inspection proved the year findings were false positives caused by matching `год` inside the genitive `года` (`1968 года`, `1975 года`). The local correction adds the missing word boundary, strengthens the glossary prompt, and repairs only a non-Russian checkpoint title while reusing all saved body chunks. With a local Russian-title substitute, the exact 17,288-character saved body passes the complete translation validator without any provider call. Targeted tests pass 21/21 and the full suite passes 400/400. No further paid continuation is authorized.
- Authorized dispatch [`29432183909`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29432183909) stopped while creating its resume lock, before the lock artifact and before produce/provider access. It made zero new OpenAI/image/AI33/Reddit/YouTube calls. The immediate parent resume correctly bound the original spend lease by canonical hash, but the builder incorrectly compared the root spend run ID with the immediate parent run ID, so a third chain link was rejected. The correction validates the already-carried `parent_spend_lease_sha256` instead, which is depth-independent; both a three-link unit proof and exact lock creation from `/tmp/acc1-resume-29431174782/` pass. Because this dispatch consumed no provider spend and created no lock, its exact authorization was reused only by follow-up run `29432517614`; it authorizes no broader retry.
- Follow-up chained continuation [`29432517614`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29432517614) passed the depth-independent lock, restored all 18 prior calls and three body chunks, then completed one Russian-title repair plus three independent translation-review calls. The journal now contains 22/48 `COMPLETE` attempts and 145,266/500,000 tokens: 120,408 input, 24,858 output, 10,752 cached input. The reviewer applied two exact unique patch sets and returned a third `REVISE` set with seven source/translation/replacement triples; the old two-revision ceiling blocked before applying that already-paid decision. Image attempts remain zero, AI33 was not initialized, and no render or YouTube path ran. Evidence is under `/tmp/acc1-resume-29432517614/`. All seven pending replacements apply uniquely and the resulting exact 17,341-character Russian body passes local translation/narration validation. The durability fix consumes a saved pending review before issuing another request, permits one third revision only for SAGA, and requires only one subsequent PASS review on the exact checkpoint; exact local resume proves one reviewer call, three revisions, and no repeated body/title generation. Conservative preflight now reserves that path. PR [#22](https://github.com/webpot-ru/nebula-core-v3/pull/22) is merged to `main` as `6572f975f904f2c9ddb931386032996ee61dae43`. No further paid continuation is authorized.
- Approved chained continuation [`29433562785`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29433562785) restored all 22 prior OpenAI calls, skipped Reddit and source discovery, passed the depth-independent resume lock, applied the seven already-paid pending corrections, and made exactly one new independent translation-review request. The preserved journal now contains 23/48 `COMPLETE` attempts and 155,346/500,000 tokens: 130,202 input, 25,144 output, 10,752 cached input, zero reasoning tokens. That review returned `REVISE` with two new exact source/translation/replacement triples after the third revision, so the fail-closed three-revision ceiling stopped before images, AI33, render, or YouTube; image attempts remain zero and no AI33 journal exists. Evidence is under `/tmp/acc1-resume-29433562785/`. Both pending replacements apply uniquely, and the resulting exact 17,320-character Russian body passes local translation/narration validation. The minimal evidence-backed correction permits a fourth revision only for SAGA and reserves the exact five-finalist pool at 27 calls under cap 48. Exact local resume of this checkpoint needs one reviewer call, reaches four revisions, and accepts a mocked PASS without regenerating the title/body or earlier reviews. PR [#24](https://github.com/webpot-ru/nebula-core-v3/pull/24) is merged to `main` as `768a2c4e2afe1d57d105dc7c0e090e97ca2078a8`. No further paid continuation is authorized.
- Approved chained continuation [`29434158451`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29434158451) restored all 23 prior calls, skipped Reddit, applied the two pending fourth-revision corrections, and made exactly one new complete review request. The journal now contains 24/48 `COMPLETE` attempts and 165,739/500,000 tokens: 139,993 input, 25,746 output, 16,640 cached input, zero reasoning tokens. The fifth full review returned six flags; several were material local fixes, but at least two proposed replacements were defensible style choices or worse Russian (`Может, и правда трогался`, `выходные ночи`). The four-revision gate therefore blocked correctly before blindly applying them. Image attempts remain zero, no AI33 journal exists, and render/YouTube were not reached. Evidence is under `/tmp/acc1-resume-29434158451/`. The correction does not add a sixth open-ended review: one final SAGA-only adjudication must classify all six flags exactly once, may apply only exact material patches, may discard a suggestion only with a reason, and returns `PASS` or `BLOCK`. Exact local replay with five safe patches and one reasoned discard uses one provider response, preserves four recorded revisions, yields a 17,347-character validated body, and produces an explicit `FINAL_PATCH_ADJUDICATION` PASS. The exact pool now reserves 28 calls under cap 48. PR [#26](https://github.com/webpot-ru/nebula-core-v3/pull/26) is merged to `main` as `4d4fe43fe1e0d1a6bbfd037771583fce271c38de`. No further paid continuation is authorized.
- Approved chained continuation [`29435307980`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29435307980) restored all 24 prior calls, skipped Reddit, and completed the single terminal adjudication exactly as designed. It applied two material fixes (`climax` and the narrator's active dispatcher role), discarded four defensible/worse suggestions with explicit reasons, and checkpointed `FINAL_PATCH_ADJUDICATION / PASS`. The journal now contains 25/48 `COMPLETE` attempts and 176,722/500,000 tokens: 150,550 input, 26,172 output, 16,640 cached input, zero reasoning tokens. The accepted Russian body is 17,327 characters. The run then failed before packaging/images/AI33/render because the unchanged 139-character translated title occupied five lines at the fixed 52 px Reddit-card title size; image attempts remain zero and no AI33 journal exists. Evidence is under `/tmp/acc1-resume-29435307980/`. The deterministic correction preserves the full title in episode data and uses a visual-only extractive display title with the opening and final hook plus an explicit middle ellipsis. Exact geometry is three lines at 52 px: `Мой начальник дал мне одно правило как диспетчеру 911: … не отвечай. Прошлой ночью я ответил.` The layout hash/report now binds the actual display title and truncation flag. PR [#28](https://github.com/webpot-ru/nebula-core-v3/pull/28) is merged to `main` as `e1132e338d7bbe222101e77af34f1339c9977a50`. No further paid continuation is authorized.
- Chained continuation [`29436132717`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29436132717) restored the accepted translation and passed the new title fitting, then failed deterministically before any provider request because a continuation Reddit page carried 61 words / 423 characters and measured 15 body lines against 12 available on the GitHub runner. Its OpenAI journal is unchanged at 25 calls / 176,722 tokens, image attempts remain zero, no AI33 journal exists, and Reddit/render/YouTube were not reached. Evidence is under `/tmp/acc1-resume-29436132717/`. The root cause is platform font-metric variance combined with the old proxy continuation limit `62 words / 440 characters`; local Arial could pass pages that Linux DejaVu wrapped into more lines. The correction uses the already-safe first-page envelope `48 words / 340 characters` for continuation pages too. Exact accepted-story replay then passes every page locally, while targeted storyboard/renderer/factory tests pass 68/68. PR [#30](https://github.com/webpot-ru/nebula-core-v3/pull/30) is merged to `main` as `57be9307671b5eb010a83d3b67433f974b9a0fd3`. No further paid continuation is authorized.
- Approved chained continuation [`29463174355`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29463174355) restored the accepted translation, passed title/body layout and made exactly one new OpenAI packaging request before failing closed. The journal now contains 26/48 `COMPLETE` attempts and 180,116/500,000 tokens: 153,379 input, 26,737 output, 16,640 cached input, zero reasoning tokens. Image attempts remain zero, no AI33 journal exists, Reddit/source stages were skipped, and render/YouTube were not reached. Evidence is under `/tmp/acc1-resume-29463174355/`. Root cause: the provider was asked both to select one of three already locked packaging options and to reproduce deterministic description/prompt/risk fields verbatim; its creative response completed, but those three redundant fields did not equal the local templates. The correction reduces the provider contract to `selected_option_index` only, then derives the locked option, exact source backing, neutral description, thumbnail prompt, language, and validated empty production risk list locally. Packaging tests pass 16/16 and the exact factory packaging handoff test passes. PR [#32](https://github.com/webpot-ru/nebula-core-v3/pull/32) is merged to `main` as `91cb8fc2248c772e13cc835a2a7821d69f6ac60e`. No further paid continuation is authorized.
- Approved chained continuation [`29464458347`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29464458347) restored 26 OpenAI calls, completed one selection-only packaging request, and reached image generation. The journal now contains 27/48 `COMPLETE` OpenAI attempts and 180,956/500,000 tokens: 154,206 input, 26,750 output, 16,640 cached input, zero reasoning tokens. One `gpt-image-2` attempt completed and saved its file; AI33 was not initialized, Reddit/source stages were skipped, and render/YouTube were not reached. Evidence is under `/tmp/acc1-resume-29464458347/`. The image provider returned a valid near-16:9 PNG at 1672x941 while the old gate demanded exact 1536x864, so the run stopped after the paid response. The correction binds the image journal/count into every new resume lease, preserves and hashes the raw provider file, deterministically normalizes only sufficiently large images within one-percent aspect-ratio tolerance, and atomically checkpoints prompt/request/raw/output hashes after every image so a chained continuation skips completed POSTs. The exact paid image normalizes to 1536x864 with the raw file retained; targeted image/resume/workflow/factory checks pass 23/23. PR [#34](https://github.com/webpot-ru/nebula-core-v3/pull/34) is merged to `main` as `bc1bbb5f85d56a989fc753485d36bbab06e52ef4`. No further paid continuation is authorized.
- Approved chained continuation [`29465754759`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29465754759) restored the exact parent evidence, made one more redundant packaging-selection call, reused the first image and completed scene images 2-4 before scene 5 was rejected by VectorEngine safety with HTTP 429. The durable OpenAI journal is 28/48 `COMPLETE` and 181,796/500,000 tokens: 155,033 input, 26,763 output, 16,640 cached input, zero reasoning tokens. The image journal has five attempts: four `COMPLETE` normalized from 1672x941 to 1536x864 and one final `AMBIGUOUS_ERROR` with no output hash. AI33 remained at zero, Reddit/source stages were skipped, render/YouTube were not reached, and no request was retried. Evidence is under `/tmp/acc1-resume-29465754759/`. The correction removes packaging provider selection entirely, chooses locked option 0 deterministically, consumes the final ambiguous scene as a checksum-bound local continuity crop/grade without another POST, and derives the thumbnail base from the first verified scene rather than buying a sixth image. Exact no-provider replay preserved 28 OpenAI and five image attempts, produced five scene assets with one local fallback and a local thumbnail; proof is `/tmp/acc1-no-repeat-proof-29465754759-k1hcth44/`. Targeted packaging/image/resume/factory/workflow checks pass 83/83. PR [#36](https://github.com/webpot-ru/nebula-core-v3/pull/36) is merged to `main` as `a5c9701ecfebbbc9403bffb124cc742d590290b5`. No further paid continuation is authorized.
- Approved chained continuation [`29466815783`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29466815783) restored the exact parent evidence and created its single-use resume lock, then stopped deterministically before any provider request because `run_produce_stage` rebuilt `episode-plan.json` with the new commit SHA while the saved image checkpoint remained bound to the prior immutable plan hash. OpenAI is unchanged at 28/48 calls and 181,796/500,000 tokens; image remains four `COMPLETE` plus one non-repeated `AMBIGUOUS_ERROR`; AI33 is absent, Reddit was skipped, and render/YouTube were not reached. Evidence is under `/tmp/acc1-resume-29466815783/`. The correction reuses a restored episode plan only after full manifest validation and permits one checkpoint plan rebind only after every saved scene request hash is revalidated. Exact no-provider replay preserved all journals, produced five scenes with one local fallback and a local thumbnail, and is under `/tmp/acc1-plan-rebind-proof-29466815783-r_8ip6tj/`. Targeted image/factory/resume/workflow checks pass 84/84. PR [#38](https://github.com/webpot-ru/nebula-core-v3/pull/38) is merged to `main` as `905de70a939dee300827b55daffdffa12725935d`. No further paid continuation is authorized.
- Approved chained continuation [`29467319852`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29467319852) passed the immutable episode-plan/checkpoint continuation, reused all OpenAI/image evidence, produced the five-scene manifest, local fallback and local thumbnail, then submitted exactly six AI33 tasks. Five audio segments completed; saved task `story_1q5rhvs__001` remained `SUBMITTED` after AI33 returned HTTP 429 `Task polling temporarily busy`. OpenAI is unchanged at 28/48 calls and 181,796/500,000 tokens; image is unchanged at four `COMPLETE` plus one non-repeated `AMBIGUOUS_ERROR`; AI33 has six/32 completed POST attempts and six unique saved task IDs. Reddit was skipped, and final concatenation/render/YouTube were not reached. Evidence is under `/tmp/acc1-resume-29467319852/`. The correction treats only this explicit temporary 429 as retryable polling, permits a resume to load the six completed AI33 journal entries without another POST, and binds both the AI33 journal and in-progress TTS state into the next lease with exact task-ID-set equality. Exact lock creation and six-attempt no-POST journal restoration pass locally; targeted TTS/resume/workflow/factory/image/packaging checks pass 101/101. PR [#40](https://github.com/webpot-ru/nebula-core-v3/pull/40) is merged to `main` as `c4147dd25b3575e27ea4bfe83946225b99aa7180`. No further paid continuation or external task poll is authorized.
- Approved bounded-poll continuation [`29468736511`](https://github.com/webpot-ru/nebula-core-v3/actions/runs/29468736511) restored the exact six AI33 POST attempts, polled only saved task `42a965e5-e9e3-4ff2-986d-4660da279143`, and completed all six audio segments without another submission. The checksum-bound final narration is 1,263.673469 seconds (21:03.673), 17,644,296 bytes, with TTS state `COMPLETE`. OpenAI remains 28 calls / 181,796 tokens; image remains four `COMPLETE` plus one non-repeated `AMBIGUOUS_ERROR`; AI33 remains exactly six POST attempts. Reddit was skipped and YouTube was absent. The run then stopped before render because the relative GitHub workdir was duplicated when the already-staged background path was resolved under artifact root; the background MP4 itself exists and matches approved SHA-256 `88e943c6c675f1327eb7020d755e312dbec3864e19dae0d021d909131c349e61`. Evidence is under `/tmp/acc1-resume-29468736511/`. The correction passes the background as `assets/chonker-reading-loop-v1.mp4`; exact artifact storyboard construction then passes with the bound background and 209 slides. Targeted factory/storyboard/workflow/TTS/resume checks pass 86/86. PR [#42](https://github.com/webpot-ru/nebula-core-v3/pull/42) is merged to `main` as `f1fc53f9f40a2388a097fd5f43c2018a1e9bbc82`. No further workflow or YouTube action is authorized.
- The local brand proposal keeps `@ChonkerTalksRussia`, the copper mascot, and the selected animated background; proposes `Chonker Talks — Истории Reddit`, a broad public description, and new banner copy. The first imagegen banner looked correct at full size but YouTube's live crop showed that its large title extended outside the all-device safe area; the user will compose the final banner. `channels.json.channel_branding.banner_asset` therefore remains `null` with status `awaiting_user_safe_area_asset`. A transparent 2560x1440 / 1546x423 guide is at `assets/acc1/branding/youtube-banner-safe-area-guide-2560x1440.png`; the local 800x800 avatar candidate remains available. Nothing has been applied or read back from YouTube.
- Both the local working tree and GitHub `main` now set every channel to `automation_enabled=false` and `videos_per_day=0`. The active legacy-publish preflight blocks every channel before credentials or provider use. Numeric `topic_mix` arrays remain candidate-scouting-only or superseded; none is an approved publishing weight.
- Twelve bounded Reddit-only reviews on 2026-07-10 proved source access but used the old committed config, so they did not validate the revised mixes.
- Twelve follow-up source-only GitHub reviews on 2026-07-11 checked the revised config from branch commit `ca6b0ad` across separate `week` and `month` windows. All runs passed credential/test/artifact enforcement, but the 63 queue entries collapsed to 19 unique posts and only two selected posts: one generic AITA story won eight runs and one link-dependent OutOfTheLoop question won four. No current `topic_mix` received `PASS`; numeric weights remain unapproved.
- The confirmed cross-channel contamination remains blocked locally for `acc4=human_drama`, `acc5=football_culture`, and `acc7=visual_comedy`. The old `acc1=dark_curiosity` forced gate is now marked `superseded_pending_rebuild`; no replacement numeric audience weights were invented. Explicit bounded `--topic-family human_drama|dark_curiosity` reviews can support SAGA research while automation stays disabled.
- GitHub gate validation on commit `0b21d75` confirmed the routing correction. `acc4` returned only `human_drama` (10 complete week candidates, 7 month); `acc7` returned only `visual_comedy` (5 week, 12 month); `acc5` returned only `football_culture` (6 week, 7 month) but remained evidence-risk; `acc1` returned no eligible `dark_curiosity` Shorts candidate in either window and failed closed instead of substituting AITA. All GitHub unit-test and credential-preflight steps passed.
- The local Russian-channel follow-up adds an isolated `shorts_from_long` path for `acc1`: select a complete 2,800-25,000 character dark source, then condense it to at most 2,200 characters only through the explicit long-to-short adapter. The adapter fails unless exact source quotes cover setup, escalation, and payoff; other channels keep the original complete-short-source rule.
- Historical Reddit-horror competitor evidence supported 45-60 minute themed compilations. The 2026-07-13 broader competitor decision supersedes that as the channel-wide default; the implementation and evidence are retained for the dark series.
- Two no-spend GitHub source reviews on commit `1206e39` tested that decision with `format_intent=long`: month run `29154057054` returned 8 eligible candidates and year run `29154057791` returned 11. All 19 were distinct, source bodies were present, and none depended on screenshots or images. The dark-story family therefore passes source availability, but automatic episode selection remains blocked: 18/19 candidates came from fiction-first `r/nosleep`, score separation was weak (all eight month candidates tied), and both selected stories ended with unresolved danger rather than a closed payoff. No numeric weight change is justified.
- Fresh long-source runs on commit `094f923` tested shorter discovery windows. Day run `29155023727` and week run `29155024651` both failed closed with no candidate meeting the current `1,000+ upvotes` and `2,800+ body characters` gates across bounded `r/nosleep` and `r/LetsNotMeet`. Long-form `acc1` scouting should therefore use `month/year` evergreen pools rather than depend on daily or weekly freshness. The existing 19-candidate pool clusters around forbidden rules in ordinary systems, family/home anomalies, night-work roles, public-space or travel traps, and haunted media/records. These are topic hypotheses, not cleared scripts or audience winners.
- User decision: every `acc1` story is sourced from Reddit. The no-invent, truth-mode, and publication holds remain. [`russian-horror-editorial-system.md`](russian-horror-editorial-system.md) is now the specialized sourcing/editorial contract for the dark series; the broader channel contract is the 2026-07-13 strategy document.
- The no-spend exact SAGA path is now live-verified on GitHub commit `e4461c3`. Snapshot A runs `29300645110` / `29300645452` / `29300645077` used the configured auto windows; snapshot B runs `29300849127` / `29300844733` / `29300845370` used `year`. Credential preflight and bounded Reddit reads worked in all six runs. Pilots 01 and 02 returned zero 2,340-3,900-word candidates in both snapshots and are `BLOCKED` for the current one-source 18-30 minute SAGA contract, not blocked by Reddit access. Pilot 03 returned two eligible month candidates and one eligible year candidate; all eligible rows were `r/nosleep` fiction, full-body, payoff-complete, and independent of screenshots, links, and native media. Pilot 03 is therefore `CHANGE` rather than channel-wide `PASS`: source availability is proven, but source diversity, visible fiction labeling, manual packaging, and human greenlight remain open. No numeric `topic_mix` weight changed.
- The first pilot 03 artifact exposed an identity drift: preliminary `story.json` / `queue.selected_post_id` selected post `1uel8us`, while deterministic review top-1 and the old draft selected `1u73jt5`. Commit `d6fbb85` makes queue identity the automatic default, requires an explicit `--post-id` for any editorial override, records the supersession, and aligns numeric-token counts through final greenlight validation. No-spend run `29301382659` live-verified the mismatch case: `queue_selected_post_id=1uel8us`, `review_top_post_id=1u73jt5`, default greenlight source `1uel8us`, `preliminary_story_superseded=false`, `DRAFT_BLOCKED`, and both production/publication flags false. The authoritative handoff is `greenlight-draft.json + queue.json`; `story.json` is explicitly preliminary.
- The approved eight-second copper-cat reading loop is imported at `assets/acc1/video/chonker-reading-loop-v1.mp4`, checksum-bound by `assets/acc1/video/chonker-reading-loop-v1.json`, and wired into both acc1 compilation workflows as the renderer background. Its source audio is always discarded; only the verified narration stream may be mapped. This wiring is merged but has not yet been exercised by a GitHub factory run.
- The compilation visual contract now schedules 3-5 source-backed visual scenes per long story. Exact editorial story beats take priority; otherwise a deterministic word-position fallback keeps one image stable within each scene instead of changing on Reddit page rollover. Workflows expose an explicit 3/4/5 visuals-per-story input. The renderer limits both the scene image and its readability shade to the left text region, feathering them before `mascot_safe_x=1040`, so the cat, face, and paws are not darkened.
- Current no-spend proof `/tmp/acc1-branded-scene-proof-v1/final-output-v2.mp4` passes compilation QA at 1920x1080 H.264/AAC, 30 fps, with exact text/timing coverage `1.0`, 362 stable cumulative-text slides / 182 pages, five scheduled visual scenes per story, final-story-only action icons, the approved cat background hash, background-audio discard, and mascot-safe compositing. The accelerated 36-second proof uses placeholder scene art and synthetic audio while exercising a 44.31-minute-equivalent source; it proves deterministic layout/QA behavior, not voice, artwork, retention, GitHub execution, production readiness, or S-tier++ quality.
- The current isolated worktree verifies 404/404 unit tests, including the unified GPT-5.4 Flex budget, spend-lease schema v5, single-use hash-bound/chained resume lock, reconciled completed-journal continuation, exact returned-tier/token accounting, resumable chunk translation with bounded Russian-title repair, pending-review consumption through the fourth SAGA revision, and fail-closed complete final-adjudication coverage, pre-gate producer/critic/playoff, normal three-PASS breadth plus the five-reviewed/95-point exceptional-winner policy, quality-first BUNDLE selection, exact weighted reviewer score instructions, BUNDLE selector and base-contract diagnostics, evidence-backed BUNDLE `week/month/year` routing, correct fail-closed link-dependency mapping, bounded Reddit request accounting, BUNDLE/THREAD/SAGA contracts, independent topic playoff, immutable hash chain, truth/safety/PII/layout gates, role-aware AI33 durability, renderer, media QA, release ceiling, private-upload boundaries, and the legacy-channel automation guard. The factory remains fail-closed and all seven channels remain held at `automation_enabled=false` / `videos_per_day=0`.
- GitHub canary `29330668895` on commit `bbfe22d` was a bounded Reddit-only factory attempt. It proved that Reddit credentials and bounded source access worked and returned roughly 100 relationship candidates, but failed before any paid provider because `reddit_media_manifest()` accessed unloaded PRAW attributes and triggered one hidden lazy fetch per candidate, exhausting the 24-request cap. PR #3 merged the fix that reads only the already-loaded submission snapshot; regression tests prove that media inspection cannot call `Submission._fetch()`. Run `29555487790` later completed the same bounded Reddit collection in 13 observed requests, live-verifying that the lazy-fetch defect is gone. No YouTube action occurred in either run; there is still no successful review-ready GitHub artifact.
- GitHub `main` now has the hardened `.github/workflows/acc1_private_upload.yml` registered as workflow `313326356`. It accepts only an exact successful factory run plus the exact successful release-gate run/hash, revalidates the bound MP4, thumbnail, metadata, media QA and acc1 OAuth mapping, and can upload exactly one private video after separate confirmation. It contains no Reddit, OpenAI, image, AI33, history-write, public or unlisted path. It is merged and locally tested but has not been live-executed; no YouTube upload has been attempted.
- PR [#1](https://github.com/webpot-ru/nebula-core-v3/pull/1) produced merge commit `8c2224b`, schema-fix PR [#2](https://github.com/webpot-ru/nebula-core-v3/pull/2) produced `bbfe22d`, PR [#3](https://github.com/webpot-ru/nebula-core-v3/pull/3) produced `f47cc0d`, and cinematic/release PR [#44](https://github.com/webpot-ru/nebula-core-v3/pull/44) produced `017934e3`. The daily factory (`312924313`), hardened private-upload workflow (`313326356`), and release-review gate (`314883599`) are active on the default branch. Initial registration run `29325460949` failed before jobs because `${{ runner.temp }}` is unavailable in job-level `env`; no credentials or providers ran. Later runs `29330668895` and `29555487790` reached bounded Reddit collection and failed before paid providers for the separately documented source defects.
- The factory-aware `acc1_release_gate.py` and no-provider review workflow are merged and active. They preserve the `READY_FOR_PRIVATE_REVIEW` ceiling and require the exact successful factory run, comparison hash, real rights evidence, and a completed human review. They cannot be used until a review-ready factory artifact exists.
- Cross-dispatch duplicate-spend and duplicate-source prevention are implemented. Before the first paid request the workflow uploads an exact source/caps/model/run-bound lease; any unexpired lease for the same episode, overlap on source ID/URL/body SHA/story signature, and every GitHub rerun fail closed. A separate single-use resume lease now restores only an adjudicated parent artifact whose OpenAI attempts are all `COMPLETE`; it does not recover ambiguous/in-flight OpenAI or image requests. AI33 writes a durable `SUBMITTING` marker before each POST, stores every task ID before polling, and resumes preserved state without resubmission. Workflow production is capped at 300 minutes, AI33 must finish within a 240-minute produce-stage deadline, and 60 minutes remain for render/QA under the 360-minute job ceiling. General recovery from timeout or ambiguous provider state remains a pre-production P1 and still requires artifact inspection rather than retry.
- The earlier `/tmp/acc1-branded-scene-proof-v1/final-output-v2.mp4` is invalid for format review: it compressed a 44.31-minute-equivalent synthetic stress fixture into 36 seconds and used a 440 Hz test tone, causing rapid page changes and an audible hum. It remains layout-stress evidence only. The current human-paced no-provider review is `/tmp/acc1-format-review-v7/format-review.mp4`: 85.445 seconds, macOS system speech, 14 timed states, visible intro/outro, exact source-style `r/subreddit`/`u/author`, deeper text-page utilization, clause-aware phrase boundaries, soft placeholder imagery without a hard horizontal edge, and separate transparent current-Reddit-style vote/comment/share pills with white outlines and content. Its story is explicitly synthetic and proves neither Reddit discovery nor topic quality; the system voice is not the configured production narrator.
- The renderer no longer invents `r/истории`, `сейчас`, `12,4 тыс.`, `Комментарии`, or `Поделиться`. Story slides carry exact subreddit/author and optional score/comment counts from the source snapshot. Missing metrics render neutral `Vote`/`Comments` labels rather than fabricated numbers. The final controls use separate transparent Reddit-style vote/comment/share pills with thin white outlines and white icon/text geometry; an award/reaction pill is omitted because the source contract has no award evidence. Controls remain final-story-only, sit directly below the completed text, and are held for at least 3.5 storyboard seconds.
- Conditional SAGA comments are not yet production-wired. The legacy fetch path truncates bodies and lacks sufficient provenance; the hardened SAGA workflows currently use `--comment-limit 0`. Competitor transcript review through vidIQ confirmed two distinct structures: question-led episodes read user responses, while narrative/updated stories and multi-story compilations do not append unrelated comments. The deterministic contract resolves `SAGA+narrative_story` to zero comments, `SAGA+question_prompt` to 2-4 selected answers, and `THREAD+question_prompt` to 8-15 responses; THREAD refuses narrative sources. Any selected response requires real IDs/permalinks/full-body hashes, top-level provenance, no truncation/deletion/external dependency, and deterministic relevance/redundancy checks. The local THREAD selector now uses multi-label episode-function diversity instead of a single-role share cap and keeps the female comment voice fail-closed with no narrator fallback.
- The isolated artifact-only lane under `specs/acc1-automated-reddit-pilot/` targets specialized dark compilations. Its provider-to-MP4 durability was proven by run `29184260452`, but that historical 54-minute artifact used only eight mostly static slides and remains below the current retention/visual contract. It is not evidence that the new one-story SAGA or THREAD production lane is publish-ready.
- GitHub carrier run `29181179414` on commit `174f73f` failed in unit tests before Reddit/provider access because Pillow was installed after tests; no provider step ran. The dependency order was fixed in `7ebbfd1`.
- Retry run `29181212503` passed all deterministic tests, Reddit credential preflight, bounded `r/nosleep` fetch, topic review, and three-story compilation selection. It selected 7,982 source words (estimated 61.4 minutes): posts `1u8m7zi`, `1ul0dpq`, and `1u73jt5`. The first Gemini full-story translation returned a long but truncated JSON object after about 170 seconds; GPT Image 2, AI33, render, and QA were skipped. Artifact `reddit-source-smoke-acc1-29181212503` is downloaded at `/tmp/acc1-run-29181212503/`.
- The confirmed routing bug was that truncated/non-JSON provider output bypassed chunk fallback because it surfaced as `VectorEngineError` rather than a parsed `complete=false`. The fix classifies only truncation/empty/JSON-parse errors as incomplete translation and preserves auth/rate failures as terminal.
- Approved retry `29181535692` on commit `73575e2` reached chunk-fallback translation but GitHub cancelled the job at its configured 10-minute ceiling (`The operation was canceled`). The artifact `reddit-source-smoke-acc1-29181535692` contains the source/selection evidence but no translation checkpoint, image, TTS, render, or QA output. The run therefore spent only Gemini translation calls; GPT Image 2 and AI33 were skipped. Root cause is orchestration durability: the carrier job timeout was too short and translation persisted only after a complete story. Follow-up commit `bd3f7eb` raises the carrier ceiling to 90 minutes and atomically checkpoints the glossary plus every completed translation chunk, resuming directly from saved chunks without repeating the failed full-story call. It passes 76 local unit tests and syntax/diff checks, is pushed to `codex/reddit-topic-source-review-20260711`, and has not been retried live. Another provider-spend retry requires explicit approval.
- Approved run `29182609994` on commit `586bcad` verified the 90-minute timeout and atomic chunk persistence. It reached the first story's independent review, then failed closed because the reviewer still returned `REVISE` after the two-revision ceiling. Artifact `reddit-source-smoke-acc1-29182609994`, downloaded at `/tmp/acc1-run-29182609994/`, preserves the first story's glossary and all four translated chunks (3,321 Russian words). GPT Image 2, AI33, render, QA, and YouTube did not run. The follow-up adds an explicit `resume_artifact_run_id` input, source-hash-validated restore of prior translation checkpoints, and a review-decision checkpoint so a later approved run can reuse the paid chunks and preserve reviewer issue evidence. This follow-up passes 77 local tests but is not live-verified; no further paid retry is authorized yet.
- Approved resume run `29183112871` on commit `191ee7e` live-verified cross-run artifact restoration and source-hash compatibility. Story `1u8m7zi` passed review after one revision and story `1ul0dpq` passed without revision; both complete story checkpoints were preserved. Story `1u73jt5` preserved its ending in all three reviews but remained `REVISE` after the two-revision ceiling because each full-document rewrite introduced or exposed different local wording defects. Artifact `reddit-source-smoke-acc1-29183112871`, downloaded at `/tmp/acc1-run-29183112871/`, contains all review decisions and issues. GPT Image 2, AI33, render, QA, and YouTube did not run. The local follow-up changes reviewer output to source-backed exact quote/replacement patches, applies only unique local replacements instead of regenerating the entire translation, and stores the current translated document in schema-v2 review state for later resume. This is locally tested but not live-verified; no further paid retry is authorized yet.
- Approved run `29183388036` on commit `89d55d7` passed all three translations/reviews, compilation packaging, three GPT Image 2 generations, and the storyboard. It stopped before any AI33 request because narration preflight correctly blocked raw clock time `3:15`; render, final QA, and YouTube did not run. Artifact `reddit-source-smoke-acc1-29183388036`, downloaded at `/tmp/acc1-run-29183388036/`, contains the accepted scripts, metadata, three images, and eight-slide storyboard. Visual inspection accepted images 2 and 3 but rejected image 1 because it contains an English written rules list despite the no-text prompt. The local follow-up converts valid Russian `HH:MM` tokens to deterministic spoken forms, restores checksum/prompt-matched prior images, and adds a stronger no-written-props guard only for rules/time concepts. A later approved resume should therefore reuse images 2/3 and regenerate only image 1. This passes 80 local tests but is not live-verified; no further paid retry is authorized yet.
- Approved run `29183978019` on commit `170a77d` live-verified clock-time normalization, selective image reuse, and regeneration of only rejected image 1. Visual inspection accepted the replacement: it has no written list, English text, or readable clock face. All translations, packaging, three images, and storyboard passed. AI33 accepted two `eleven_v3` tasks: the first chunk completed with a checksum-bound MP3, while polling the second saved task ID returned HTTP 500 with `retryable=true`; 14 chunks were never submitted. Render, final QA, and YouTube did not run. Artifact `reddit-source-smoke-acc1-29183978019`, downloaded at `/tmp/acc1-run-29183978019/`, preserves the 16-chunk TTS plan, one COMPLETE chunk, one SUBMITTED task, and the finished audio file. The local follow-up restores TTS state/audio cross-run and retries transient AI33 polling errors with bounded backoff without resubmitting saved task IDs. This passes 81 local tests but is not live-verified; no further paid retry is authorized yet.
- Approved run `29184260452` on commit `37b2a4e` is the first complete provider-to-MP4 artifact-only pass. All three source-preserving translations/reviews, packaging, checksum-bound images, eight-slide storyboard, 16 AI33 `eleven_v3` chunks, audio concatenation, 1920x1080 H.264/AAC render, and fail-closed compilation QA passed. The final video is 3,253.777 seconds (54:13), 122,419,630 bytes, H.264 video plus 44.1 kHz stereo AAC; QA reports `PASS`, zero failures/warnings, three stories, eight slides, and a 51.79-minute source estimate. Artifact `reddit-source-smoke-acc1-29184260452` is downloaded at `/tmp/acc1-run-29184260452/`. No YouTube or publication step ran. Frame inspection at intro, story sections, and outro found clean 16:9 composition and readable title/CTA, but also proved the current visual lane is not publication-ready: only eight mostly static slides cover 54 minutes. Pipeline durability and technical artifact integrity have reached A-range evidence; visual storytelling/retention remains BLOCKED below A- until the storyboard has scene-level visual cadence and actual motion/shot variation.
- Approved run `29186076815` on commit `116be13` live-verified the bounded emotion-video proof with exactly one acc1 narrator AI33 request. It used `eleven_v3`, emotional tags, creative settings, and `with_transcript=true`; YouTube mapping was skipped and no Reddit, Gemini, Image2, render-publish, or upload path ran. AI33 returned 52 usable timed words from 0.14s to 24.399s, so the 24.8-second 1920x1080 H.264/AAC proof reports `timing_source=ai33`, not an estimated fallback, with seven caption phrases. GitHub artifacts are downloaded at `/tmp/acc1-emotion-run-29186076815/`. Frame inspection found the first render too dark and one phrase split poorly; a no-spend local rerender now uses a lighter grade, smaller captions, and punctuation-aware phrase boundaries. The improved MP4 is `/tmp/acc1-emotion-run-29186076815/improved/emotion-video-sample.mp4`. Local tests now pass 84/84; the improved renderer has not yet been re-run in GitHub, but it uses the exact downloaded AI33 audio/timings and adds no provider dependency.
- Bounded AI33 alternate-narrator run `29190123861` on commit `7efa7bc` generated one Russian emotional sample with David (`elevenlabs_cCYjmrGZaI86GUJ7F2Nn`), explicit `eleven_v3`, the `creative` profile, and provider word timings. The MP3 is 27.951 seconds, 44.1 kHz stereo and is downloaded under `/tmp/ai33-david-29190123861/voice/`; the paired local-format proof is under `/tmp/ai33-david-29190123861/video/`. YouTube mapping was disabled and no Gemini, Reddit or publication path ran. Failed precursor run `29190054352` stopped at AI33 validation before audio generation because metadata returned a raw ElevenLabs ID without the provider prefix; the manual-sample override now validates IDs and adds `elevenlabs_` for raw catalog IDs without changing `channels.json`. This sample is listening evidence only and does not change the configured acc1 narrator.
- User-confirmed Russian voice roles are now configured in `channels.json`: `elevenlabs_JBFqnCBsd6RMkjVDRZzb` is the male primary narrator and `elevenlabs_MOgsVr0EwwxqQs5cNDhu` is the female comment voice. Both use provider speed `1.0` and plain narration without Eleven v3 emotion/audio tags. The earlier `1.10x`/`1.20x` local files and tagged samples are comparison artifacts only, not production settings.
- GitHub run `29217400229` on commit `aeb83ef` generated one bounded plain-horror sample for `elevenlabs_MOgsVr0EwwxqQs5cNDhu` with explicit `eleven_v3`, provider speed `1.0`, default voice settings, no emotion/audio tags, and usable AI33 word timings. The user identified this as the female voice, so it is comment-role evidence, not an approved narrator sample. YouTube mapping, Gemini and Reddit were not run. The downloaded artifact is under `/tmp/chonker-mogs-run-29217400229/`; a male `JBF...` narrator sample has not yet been generated.
- The selected acc1 background originated from the user-supplied eight-second loop `/Users/lali/Downloads/Cat_reading_story_text_202607131032.mp4`. Frame review confirmed a stable copper mascot and props, left-directed reading gaze, clean blink, closed mouth, clear left text field, and matching loop endpoints. The checksum-identical project copy now lives at `assets/acc1/video/chonker-reading-loop-v1.mp4`; both local compilation workflows stage it into the artifact root and the renderer ignores its source AAC. GitHub execution of this new wiring and a separately approved male `JBF...` narrator sample are still pending.
- `compilation_storyboard.py` and `compilation_renderer.py` now implement the production-oriented Reddit-pages surface: precomputed cumulative text that does not move or flash, title/meta only on the first page, continuation pages without the title, optional checksum-bound looping background under the artifact root, pixel-measured Cyrillic wrapping, and no opaque card. Upvote/comment/share actions are shown exactly once after the final chunk of each story segment and are forbidden on intro, transitions, intermediate story chunks, and outro. `compilation_qa.py` binds the accepted narration, creative manifest, background, MP4, configured voice, and 1280x720+ thumbnail by checksum and fails closed on timing/coverage/action/background mismatch. The corrected no-spend synthetic proof is `/tmp/acc1-visual-hardening-proof-v2/proof.mp4`; inspected frames are `story-building.png`, `story-complete.png`, and `outro-no-actions.png`. This proves implementation geometry, not the final cat-background art direction.
- Direct browser inspection of Saranago Boy video `PMnk1PkrsO0` established the base geometry: fixed precomputed wrapping, progressive word chunks without whole-text fade, and avatar/username/time only on page one. The current Chonker-right / text-left variant uses official Reddit Sans at meta/title/body/action sizes 28/46/48/27 px, a 44-character measure, explicit 62 px body-line steps, and reduced 520/640-character page capacities. The vote/reply/share/overflow row appears only during the final 3.2 seconds and is positioned dynamically 84 px below the last body line instead of at the screen bottom. The superseded geometry proof was moved to `Trash/20260713Tcleanup-story-image/tmp-cleanup-staging/20260713-story-image-tests/chonker-copper-layout-v3/`; it used existing female `MOgs...` audio only to validate geometry, not narrator-role selection.
- The optional story-image layer is now local-verified in the same proof renderer. A still can fade and move slowly behind only the left 1180x1080 text region while the animated mascot remains unobscured on the right and the Reddit text stays in the final ASS layer. The accepted test uses 70% image opacity, brightness `-0.10`, saturation `0.78`, blur `1:1`, 0.8-second fades, a slow center zoom, and a 320 px right-edge alpha feather from x=760 to x=1080 so the image does not darken the mascot's left paw. At the user's request the 1920x1080 H.264/AAC proof, comparison frames, and CC0 hallway fixture were moved to `Trash/20260713Tcleanup-story-image/tmp-cleanup-staging/20260713-story-image-tests/`. This verifies layout/compositing only and used the existing female `MOgs...` audio, not the pending male channel voice.
- The pilot specification now includes a required audience-learning loop rather than treating upload volume as success. Each episode will carry an experiment/archetype manifest, three honest packaging angles, scene time ranges, immutable 24h/72h/7d/28d analytics snapshots, and scene-level retention mapping. Initial targets remain relative to the first six same-channel long-form pilots; an archetype is not promoted from one outlier and needs at least two wins across three distinct stories. Analytics ingestion and YouTube/Studio readback are specified but not implemented or live-verified yet.
- `acc4` and `acc7` remain the other Reddit-card pilot candidates. `acc1` has a locally valid BUNDLE/SAGA/THREAD strategy, bounded source selectors, a 3-5-candidate topic gate requiring at least three passing finalists, role-aware media pipeline, renderer/QA surface, and brand proposal but remains publication-disabled. Pilots 01/02 now use BUNDLE rather than the disproven one-source SAGA contract; pilot 03 remains SAGA; THREAD 04-06 still need the first exact live collector/artifact proof. `acc2`, `acc3`, `acc5`, and `acc6` require the evidence-dossier lane before publishing.

- `index.html`, `style.css`, `app.js` implement the local RedditSim recorder UI.
- `scraper.py` fetches candidate Reddit stories through PRAW OAuth2, weighted topic-family source planning, network-wide duplicate/signature/similarity guards, channel `topic_exclusions`, velocity scoring, topic-fatigue penalties, and a bounded Gemini producer-quality gate, then writes `story_data.json` and `producer_queue.json`. Disabled channels fail before Reddit access unless an approved review passes `--allow-disabled-channel`; cross-channel reuse requires the explicit `--allow-cross-channel-reuse` escape hatch. The producer prompt now prefers each channel's `viewer_promise` and `producer_brief` over language-wide defaults.
- `scraper.py` now requires explicit `REDDIT_CLIENT_ID` and `REDDIT_CLIENT_SECRET` environment credentials; `REDDIT_USERNAME` and `REDDIT_PASSWORD` are an optional all-or-nothing script-OAuth pair. It never falls back to credential material embedded in source. `--check-reddit-config` is a no-network preflight and `--no-save-history` keeps bounded candidate reviews from changing `published_history.json`.
- `story_adapter.py` is the selected-story editorial layer. It calls the shared Gemini text provider after scraper selection and before metadata/translation, tightens the story without inventing facts, can move a source-backed hook into the opening, preserves URLs/timeline/point of view, and fails in strict mode when hook evidence is not found in the source title/body/comment text.
- For `acc1` Shorts only, `story_adapter.py --long-to-short --max-body-chars 2200` may condense a complete long dark source. This is not raw truncation: it requires source-backed setup/escalation/payoff evidence and rejects an over-limit or unsupported retelling.
- `translator_tts.py` localizes non-English `story_data.json` through the shared Gemini text provider, keeps translated display fields for storyboard/card rendering, prepares separate narration-only fields where raw URLs are spoken as localized on-screen-link phrases and Russian numeric tokens are expanded into spoken words, then submits narration text to AI33 TTS v3. Full-story translation now allows 16,384 output tokens by default instead of 4,096; override it with `--translation-max-output-tokens` or `GEMINI_TRANSLATION_MAX_OUTPUT_TOKENS`. Channels can use separate `tts_voice` and `comment_tts_voice` roles; the script concatenates role segments into one `narration.mp3`. The higher live response size has not been provider-tested in this change.
- `metadata_generator.py` generates YouTube packaging metadata through the shared Gemini text provider. It now treats tags as secondary, asks for three honest title/thumbnail/first-screen options, stores them in `packaging_options`, and writes the selected option into backward-compatible title/thumbnail fields. It also has an explicit `--fallback-on-error` mode for render dry-runs: if Gemini blocks the metadata prompt, it writes conservative marked fallback metadata so MP4/audio/render QA can still run. Production upload workflows should stay fail-closed unless this flag is intentionally added.
- `thumbnail_generator.py` can either add deterministic Cyrillic text to an existing local `--base-image` with a checksum/dimension `--report` and no provider call, or generate a new image through VectorEngine only with explicit `--confirm-spend`.
- `storyboard_generator.py` creates a deterministic no-API `storyboard.json` from `story_data.json`. It now emits `render_slides`: story text is split into screen-sized chunks, first story chunks show header/title but no footer when the story continues, middle story chunks show only continuation text, and only the final story chunk shows post upvotes/comments/share. Comment continuations follow the same rule for comment header/footer. Long stories advance by replacing the card content rather than scrolling, and short final orphan chunks are merged back when they fit.
- `render.py` opens the existing RedditSim UI in headless Chrome/Chromium, captures deterministic clean slide screenshots, and uses FFmpeg to render an MP4 from `storyboard.json`. In default `--orientation auto` mode, videos up to 180 seconds render vertical 9:16 (`1080x1920`, mobile layout), while videos longer than 180 seconds render horizontal 16:9 (`1920x1080`, desktop layout). Both orientations render clean Reddit cards with editor/sidebar widgets hidden. If `narration.mp3` exists, it merges that file as an AAC audio track. Current workflows pass `--no-karaoke`, so transcript timings are used only to switch static slides at `word_start` boundaries; no yellow karaoke highlight is rendered. If usable timings are unavailable, the renderer falls back to word-weighted slide boundaries. `--report render_report.json` writes render readback for the QA gate.
- `pre_publish_qa.py` is the fail-closed local/upload gate. It checks final MP4 audio/video streams, audio/video duration match, render report state, story adaptation, hook evidence, narration URL sanitization, storyboard slides, metadata title length, and channel language before upload. Karaoke checks run only when explicitly requested with `--require-karaoke`.
- `scripts/move-to-trash.sh` is the project safe-trash helper. Generated previews and scratch files must be moved to `Trash/<timestamp>/...`; agents must not use direct deletion commands.
- `uploader.py` is a YouTube Data API uploader with a fail-closed channel-token preflight against `channels.json` and post-upload metadata readback.
- `.github/workflows/auto_publish.yml` runs the cloud publish pipeline. It verifies the YouTube refresh token against `channels.json` before Reddit/Gemini/AI33/render work. The first full live smoke succeeded as an unlisted upload; the later OAuth/channel-mapping blocker is reported resolved by the user as of 2026-07-02, but the fail-closed preflight remains mandatory before any content-generation spend or upload.
- `.github/workflows/video_dry_run.yml` is currently a live manual workflow: it fetches Reddit content, can spend Gemini/AI33 quota or credits for quality/adaptation/metadata/localization/TTS, renders `final_output.mp4` with clean no-karaoke visuals, runs `pre_publish_qa.py`, and uploads story, producer queue, metadata, audio, transcript, render report, QA report, video, and previews. Manual dry-runs now support `topic_family`, `video_slot`, `content_format`, and `voice_settings_profile`; use `content_format=shorts` to exercise live search/filtering while forcing a vertical artifact without YouTube upload, and use `voice_settings_profile=creative` only for intentional expressive-voice tests.
- `.github/workflows/reddit_source_smoke.yml` supports forced family/time/source bounds, exact `acc1_pilot_id`, `format_intent`, and a review label; preserves bounded queue source bodies; and records git SHA, `channels.json` SHA-256, exact inputs, and a config snapshot. In default source-only mode it forces AI quality off, writes no history, calls no Gemini/AI33/image/render/YouTube path, and emits a checksum-bound full-body review plus a `DRAFT_BLOCKED` SAGA greenlight template. Exact-pilot routing was live-verified by runs `29300645110`, `29300645452`, `29300645077`, `29300849127`, `29300844733`, and `29300845370`; the identity-handoff fix was live-verified by `29301382659`. Its optional `run_compilation_pilot=true` branch remains a separate explicitly spend-gated provider/render lane and was skipped in every listed run.
- `scripts/review_reddit_topics.py` preserves the earlier generic `SHORTLIST_FOR_RIGHTS_REVIEW` discovery mode and adds `deterministic_full_body_saga` for exact acc1 pilots. The SAGA mode verifies canonical pilot/subreddit routing, 2,340-3,900 source words, full-body hash, viewer-promise pillar, truth mode, payoff evidence, and independence from links, Markdown images, and native Reddit media before returning `SAGA_SOURCE_ELIGIBLE_FOR_GREENLIGHT`. Scraper, review, and greenlight now count numeric tokens identically, preventing dates/ages/amounts from breaking the hash-bound handoff. Neither status authorizes production.

## GitHub Dry-Run Status

- Dry-run renderer commits were pushed to `origin/main` and transferred to `webpot-ru/nebula-core-v3`.
- GitHub Actions run `28421055129` succeeded on 2026-06-30 under the `webpot-ru` account.
- The `render-dry-run` job completed in 1m 21s, passed validation tests, and successfully uploaded the `chonkertalks-dry-run-video` artifact.
- The artifact was downloaded locally to `build/render/chonkertalks-dry-run-video` and contains `final_output.mp4` (594 KB, verified codec and resolution).
- The transition from `startup_failure` to success was resolved by migrating repository ownership to `webpot-ru` (which has a healthy billing profile) and setting all required repository secrets.
- Manual `video_dry_run.yml` run `28699752190` succeeded on 2026-07-04 for `acc1`, `time_filter=auto`, `topic_family=channel_mix`, `video_slot=1`, and `content_format=shorts`. It selected `r/nosleep` post `1uh3bc5` ("There's an invisible line in my town that no animal will cross."), localized/adapted it to Russian as "В нашем городе есть невидимая линия, которую не перейдет ни одно животное", generated `narration.mp3`, rendered a vertical Shorts MP4, and uploaded artifact `chonker-test-acc1`.
- The run `28699752190` artifact was downloaded locally to `build/github-artifacts/run_28699752190/chonker-test-acc1`. `pre_publish_qa.json` passed with `status=ok`, `0` failures, and `0` warnings. `ffprobe` verified `final_output.mp4` as H.264 `1080x1920` at 30 fps with AAC audio; video duration was `30.700s`, audio duration was `30.682s`, and QA measured audio/video mismatch at about `0.006s`.
- `render_report.json` for run `28699752190` confirmed `renderFormat=shorts`, `resolution=1080x1920`, `audio=narration.mp3`, `frameSchedule=uniform_progress`, and `karaokeEnabled=false`. The artifact contains no `narration.json`. Visual preview readback (`preview_01.png`, `preview_mid.png`, `preview_end.png`) showed clean static Reddit cards with no karaoke highlight and no editor/sidebar controls; the post footer appears only in the final preview frame.
- Manual `video_dry_run.yml` run `28703717316` succeeded on 2026-07-04 for `acc1`, `time_filter=auto`, `topic_family=channel_mix`, `video_slot=1`, and `content_format=shorts` after the source-length / full-body quality-gate fixes. It selected `r/AITAH` post `1uh7cbj` and localized/adapted it to Russian as "Сдаю плазму ради аренды, пока падчерица копит на бэби-шауэр". The selected source body was `2301` chars under the Shorts policy `250-2400`, with `content_limits=null`, so the story was selected as a complete short source rather than trimmed.
- The run `28703717316` artifact was downloaded locally to `build/github-artifacts/run_28703717316/chonker-test-acc1`. `pre_publish_qa.json` passed with `status=ok`, `0` failures, and `0` warnings. `ffprobe` verified `final_output.mp4` as H.264 `1080x1920` at 30 fps with AAC audio; video duration was `96.866667s`, audio duration was `96.862041s`, and QA measured audio/video mismatch at about `0.0046s`. `render_report.json` confirmed `renderFormat=shorts`, `resolution=1080x1920`, `audio=narration.mp3`, `frameSchedule=uniform_progress`, and `karaokeEnabled=false`; the artifact contains no `narration.json` by design.
- Visual readback for run `28703717316` showed clean static Reddit cards with no karaoke highlight and no editor/sidebar/topbar controls. Direct MP4 frame extraction at `45s` showed the second story slide without header/footer, at `70s` showed the final story slide, and `preview_end.png` showed the post footer metrics only at the end. Metadata and localization readback showed `source=google-gemini`, `language=ru`, and `comments=0` for this Shorts run.

## Live Publish Smoke Status

- Manual `auto_publish.yml` run `28441721783` succeeded on 2026-06-30 for `acc4`, `video_slot=1`, `time_filter=auto`, `topic_family=human_drama`, `privacy_status=unlisted`.
- The selected story was `r/AmItheAsshole` / `top/week`: "AITA for being blunt with my boyfriend about why he isn't getting hire..." with about 7.3k upvotes and 1.5k comments. Gemini quality gate approved it as `REWRITE` after skipping one malformed-JSON quality response.
- Localization/TTS succeeded using the old Spanish Edge-placeholder voice that was configured at the time; this was before the channel voice migration to ElevenLabs. `story_data.json` was localized to Spanish (`es-419`), `narration.mp3` was produced, and `narration.json` was saved.
- Render succeeded, but exposed a format bug: `final_output.mp4` was 1080x1920 for a 237.672s video. Videos longer than 180 seconds should be horizontal, so the renderer now switches `--orientation auto` outputs over 180 seconds to 1920x1080.
- Upload succeeded as unlisted with YouTube video id `cFX2tZmLrAs`; public oEmbed readback returned the Spanish title "¿Soy la mala por decirle a mi novio la verdad de por qué nadie lo contrata?".
- Historical blocker: public/user readback showed videos landing on the wrong channel for the requested account. The user reports that all seven OAuth credentials and scopes have now been reissued and verified, so this is no longer the active blocker; keep `uploader.py` channel preflight enabled so any future mismatch fails before spend/upload.
- The workflow committed the published-history update to `origin/main` as `d54da14 chore: history [acc4] slot=1 [skip ci]`.
- Manual `auto_publish.yml` run `28693621091` succeeded on 2026-07-04 for `acc1`, `video_slot=1`, `time_filter=auto`, `content_format=shorts`, `privacy_status=unlisted`. It uploaded to the verified `ChonkerTalks Россия` account as video id `LgURZlQqj10`; upload readback reported `privacy=unlisted`, `language=ru`, and channel id `UCNSxg53AGM4WstRjGiQdS8w`. Render report showed 45.531s vertical Shorts output (`1080x1920`), AAC audio from `narration.mp3`, `karaokeEnabled=true`, `transcriptWordCount=115`, and `frameSchedule=karaoke_phrase_times`. `pre_publish_qa.py` passed with `0` failures, `0` warnings, and karaoke coverage `0.9914`. The workflow committed the published-history update to `origin/main` as `c023eb9 chore: history [acc1] slot=1 [skip ci]`.

## Verified Locally

- On 2026-07-10, `python3 -m unittest tests/test_scraper_reddit_config.py tests/test_channel_strategy.py` passed all 10 tests. The tests cover secret-free Reddit credential wiring, unique channel promises, fail-closed strategy fields, topic-mix normalization, config-driven producer briefs, disabled-channel preflight, and network-wide duplicate protection.
- `python3 -B -m py_compile scraper.py story_adapter.py metadata_generator.py` passed on 2026-07-10.
- `channels.json` parses as valid JSON. All seven viewer promises are non-empty and unique, all channels are automation-disabled, and every candidate-scouting mix still sums to 1.0 while being explicitly marked unvalidated or superseded.
- `node --check app.js` and `python3 -B -m py_compile storyboard_generator.py render.py translator_tts.py pre_publish_qa.py` passed on 2026-07-04 after the static timed-slide / no-karaoke render update.
- Browser localhost QA passed on 2026-07-04 for the deferred final post footer in both target render sizes: vertical 1080x1920 mobile layout and horizontal 1920x1080 desktop layout. In both checks, the final story slide started with `meta-deferred` / hidden footer and changed to `meta-revealed` / visible footer on the final karaoke phrase; editor/sidebar/nav controls were hidden and browser console logs were clean.
- `python3 -m py_compile storyboard_generator.py render.py translator_tts.py scraper.py` passed on 2026-07-02.
- `python3 storyboard_generator.py --input sample_story_data.json --output build/render/slide_storyboard.json` passed on 2026-07-02 and produced 4 slide-backed scenes from the safe sample fixture.
- `python3 render.py --storyboard storyboard.json --output final_output.mp4` passed once locally through the RedditSim Chrome DevTools path and captured 8 simulator frames.
- A 2026-07-04 local no-spend render using `build/github-artifacts/run_28703717316/chonker-test-acc1/story_data.json` plus the existing `narration.mp3` passed after the timed-slide/background update: `/tmp/reddit_timed_static_bg.mp4` rendered as H.264 `1080x1920` with AAC audio, `durationSec=96.862041`, `karaokeEnabled=false`, `frameSchedule=static_slide_boundaries`, `slideTimingSource=word_weighted`, and two story slides. Extracted frames at 1s, 48s, and 90.2s showed wider Reddit-like typography, denser card usage, subtle dark grain/vignette background, no editor controls, no karaoke highlight, and the footer only at the final reveal.
- A synthetic transcript timing check on 2026-07-04 confirmed that when `narration.json` word timings are present, `render.static_slide_frame_plan()` uses the next slide's `word_start` timestamp as the static slide boundary and reports `slideTimingSource=transcript`.
- A synthetic AI33 subtitle payload check on 2026-07-04 confirmed that inline SRT/VTT text is parsed into `narration.json` words with `timing_source=subtitle`, `timing_status=ok`, and usable `start`/`end` values for render slide timing. This check did not call live AI33 or spend provider credits.
- FFmpeg encoding was independently verified from the captured RedditSim frames: H.264, 1080x1920, 30 fps, 22.0s, 660 frames.
- `test -s final_output.mp4` passed on local generated artifacts, and the current GitHub no-karaoke artifact `build/github-artifacts/run_28699752190/chonker-test-acc1/final_output.mp4` was downloaded and verified with `ffprobe`.
- `app.js` passes `node --check` on 2026-07-02.
- RedditSim loaded in a real browser at `http://localhost:8080/`; the typewriter animation completed and rendered title, body, and comments.
- Browser console only showed missing `favicon.ico` 404s during that smoke, not JavaScript runtime errors.

## AI33 TTS State

- `translator_tts.py` now uses `POST https://api.ai33.pro/v3/text-to-speech` with multipart FormData and `xi-api-key`.
- `translator_tts.py` sends `model_id=eleven_v3` by default; override with `--model-id` or `AI33_TTS_MODEL_ID` only intentionally.
- The local TTS model contract is now fail-closed: `--require-model-id eleven_v3` rejects a different requested model before any AI33 call, every successful/submission-only request writes sanitized `tts_request_metadata.json`, and `narration.json` records `requested_model_id`. If AI33 explicitly reports a different model in its task payload, generation fails after preserving the mismatch metadata. When AI33 does not report a model, the artifact says `provider_not_reported` rather than claiming server-side confirmation.
- `auto_publish.yml` and `video_dry_run.yml` pass `--model-id eleven_v3 --require-model-id eleven_v3` explicitly. `pre_publish_qa.py --require-tts-model eleven_v3` requires the safe audit artifact, and the dry-run artifact includes it. The model preflight/sample path was verified by paid GitHub AI33 A/B runs `29153420437` (`default`) and `29153423227` (`creative`) on commit `a810197`.
- `translator_tts.py` can pass optional AI33/ElevenLabs `voice_settings` through `--voice-settings-json` or `AI33_VOICE_SETTINGS_JSON`. This is intentionally off by default until short sound tests confirm that the chosen settings improve expression without hurting word timings.
- `scripts/generate_ai33_voice_samples.py` now supports emotion-focused sound tests without changing production publishing: `--text-style emotional` adds sparse Eleven v3 audio tags such as `[curious]`, `[sighs]`, and `[whispers]`; `--voice-settings-profile creative` sends `{"stability":0.35,"similarity_boost":0.75,"style":0}` for a more expressive first pass; `natural` uses stability `0.50`; `robust` uses stability `0.70`; `default` omits provider settings. Dry-run mode prints the exact sample payloads without spending credits.
- The expected secret is `AI33_API_KEY`; `A133_API_KEY` is accepted only as a compatibility fallback for older local notes.
- `channels.json` now uses ElevenLabs-prefixed AI33 voice ids for all seven channels. Edge voice ids remain only historical placeholders and should not be used for production publishing.
- Raw ElevenLabs candidate ids provided from AI33 Voice Library screenshots/readback must be written with AI33 prefixes in this repo, for example `elevenlabs_cCYjmrGZaI86GUJ7F2Nn`. Currently documented candidates include active pairs for Russian, English, German, LATAM Spanish, Brazil Portuguese, French, and Italian, plus spare/alternate voices.
- Voice selection is per channel and per role; there is no need to find one universal voice for every language. `acc4` now has a Spanish Latin-accent narrator/comment pair in `channels.json`: `elevenlabs_22VndfJPBU7AZORAZZTT` for title/body narration and `elevenlabs_8mBRP99B2Ng2QwsJMFQl` for comments. AI33 metadata readback confirmed both as Spanish `es-AR` with `latin american` accent. This still needs a short AI33 sound test before public use.
- `acc5` now has a Brazilian-accent narrator/comment pair in `channels.json`: `elevenlabs_dX7gRq1dIvLTgUaWpEFn` for title/body narration and `elevenlabs_4r3G9XKliGgVZLKMgjik` for comments. AI33 metadata readback confirmed both as Portuguese `pt-BR` with `brazilian` accent. This still needs a short AI33 sound test before public use.
- The earlier `acc1` pair `elevenlabs_rQOBu7YxCDxGiFdTm28w` / `elevenlabs_ymDCYd8puC7gYjxIamPt` remains historical metadata-readback evidence only. The user-selected active pair in `channels.json` is now `elevenlabs_JBFqnCBsd6RMkjVDRZzb` for the male narrator and `elevenlabs_MOgsVr0EwwxqQs5cNDhu` for female THREAD/comment narration, at speed `1.0` without emotion tags.
- `acc3` now has a standard German narrator/comment pair in `channels.json`: `elevenlabs_aTTiK3YzK3dXETpuDE2h` for title/body narration and `elevenlabs_LB5G0Z4EP98YaEgL654m` for comments. AI33 metadata readback confirmed both as German `de-DE` with `standard` accent. `elevenlabs_5KvpaGteYkNayiswuX2h` is documented as a spare older German male voice. This still needs a short AI33 sound test before public use.
- `acc2` now has a US English narrator/comment pair in `channels.json`: `elevenlabs_sB7vwSCyX0tQmU24cW2C` for title/body narration and `elevenlabs_DODLEQrClDo8wCz460ld` for comments. AI33 metadata readback confirmed both as English `en-US` with `american` accent. `elevenlabs_nzFihrBIvB34imQBuxub` is documented as a spare young English male voice. This still needs a short AI33 sound test before public use.
- `acc6` now has a France-standard French narrator/comment pair in `channels.json`: `elevenlabs_wufFsVwuYBePWKO6dMMN` for title/body narration and `elevenlabs_i6ke7jvmGEVUyV4zjSaT` for comments. AI33 metadata readback confirmed `wuf...` as French `fr-FR` standard male and `i6...` as French `fr-FR` Parisian female. `elevenlabs_93nuHbke4dTER9x2pDwE` remains a Québec male spare. This still needs a short AI33 sound test before public use.
- `acc7` now has a standard Italian narrator/comment pair in `channels.json`: `elevenlabs_ImsA1Fn5TNc843fFdz99` for title/body narration and `elevenlabs_RXoaSpLaWTEckJgPUBG3` for comments. AI33 metadata readback confirmed both as Italian `it-IT` with `standard` accent. This still needs a short AI33 sound test before public use.
- `.github/workflows/voice_metadata_check.yml` and `scripts/check_ai33_voice_metadata.py` provide a no-audio readback path for AI33 voice metadata through the repository `AI33_API_KEY` secret. The workflow uses `GET /v3/voices?provider=elevenlabs&search=<voice_id>`, prints only sanitized metadata for requested voice IDs, and must not call `/v3/text-to-speech`.
- Current remaining voice coverage: all seven channels have narrator/comment pairs configured. Remaining blocker is sound quality verification, not missing catalog coverage.
- Live AI33 v3 smoke passed on 2026-06-29 using the gitignored LUNA2 local env key as a one-off user-approved read. The secret was not printed or copied into this repo.
- The first smoke submitted an ElevenLabs-prefixed voice id (`elevenlabs_21m00Tcm4TlvDq8ikWAM`) with text containing `[sighs]`, `[laughs]`, and `[whispers]`; AI33 returned `task_id=3f489e0b-3b73-40c2-95fd-071ee694055c`, task polling returned `status=done`, and `/tmp/reddit_ai33_laugh_sigh.mp3` was written.
- A second smoke explicitly sent `model_id=eleven_v3` with `[laughs]` and `[sighs]`; AI33 returned `task_id=08c146ad-82a0-4efb-a4e2-f8ec65254852` and wrote `/tmp/reddit_ai33_eleven_v3_laugh.mp3`.
- `afinfo` verified the explicit `eleven_v3` smoke output as a 5.64s MP3, 44.1 kHz stereo, 128 kbps, 90,740 bytes.
- AI33 v3 task polling uses `/v3/task/{task_id}` with `Authorization: $AI33_API_KEY` by default. If account-specific routing differs, set `AI33_TASK_URL_TEMPLATE` or `AI33_TASK_AUTH_HEADER` before running TTS.

## Gemini / VectorEngine State

- Text Gemini calls now prefer direct Google Gemini through `GOOGLE_GEMINI_API_KEY`, `GEMINI_API_KEY`, or `GOOGLE_API_KEY`. The client uses `x-goog-api-key` against `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`, default `model=gemini-3.5-flash`.
- GitHub Secrets readback on 2026-07-04 confirmed `GOOGLE_GEMINI_API_KEY` is present, updated at `2026-07-04T05:59:44Z`. Once the Google-first routing changes are pushed, cloud workflows should use direct Google Gemini for text calls and keep `VECTORENGINE_API_KEY` as fallback/image provider.
- If no Google Gemini key is present, `vectorengine_client.py` falls back to the LUNA2-compatible VectorEngine env names `VECTORENGINE_API_KEY`, `VECTOR_ENGINE_API_KEY`, and optional `VECTORENGINE_BASE_URL`.
- Set `GEMINI_PROVIDER=google` or `GEMINI_PROVIDER=vectorengine` only when intentionally forcing a provider. In normal CI/local use, leave it unset so Google Gemini is used when configured and VectorEngine remains a fallback.
- `vectorengine_client.py` retries Gemini JSON calls on transient request/API/empty-text failures; override with `GEMINI_RETRIES` or legacy `VECTORENGINE_GEMINI_RETRIES` if needed. This protects publish runs from one-off empty-text responses during translation or metadata generation.
- In `GEMINI_PROVIDER=auto` mode, `vectorengine_client.py` now falls back to VectorEngine Gemini immediately when Google returns HTTP 429 and a VectorEngine key is configured, instead of waiting through Google retry delays. This keeps live dry-runs from stalling on Google free-tier request bursts while preserving explicit `GEMINI_PROVIDER=google` as Google-only.
- `thumbnail_generator.py` uses the LUNA2-compatible image endpoint shape: `POST /v1/images/generations`, default `model=gpt-image-2`, size `1536x864`.
- Live metadata smoke passed on 2026-06-29 using the gitignored LUNA2 VectorEngine env file as a one-off user-approved read. The secret was not printed or copied into this repo.
- Smoke command wrote `/tmp/reddit_vectorengine_metadata_live.json` with `source=vectorengine-gemini`, `model=gemini-3.5-flash`, `channelId=acc4`, `language=es-419`, 79-character Spanish title, 323-character description, 8 tags, 4 hashtags, thumbnail text, thumbnail prompt, SEO keywords, and no risk flags.
- Thumbnail image generation has only been dry-run checked in this repo. It is connected but not live image-spend verified.

## Current Pipeline Changes

- `translator_tts.py` now translates `title`, `body`, and each comment `body` for non-English channels before TTS. By default it overwrites `story_data.json` so downstream storyboard/render steps see localized text; use `--translated-story-output` to write a separate localized file.
- The default narration order mirrors visible card text: title, body, then comment bodies. For raw URLs and service link lines, visible fields keep the actual link on the card while `narration_title`, `narration_body`, and `comments[].narration_body` say the localized "link on screen" phrase. For Russian narration, visible numeric tokens stay unchanged in display fields while the narration-only fields spell numbers out for TTS, for example `6500+` -> `более чем шесть тысяч пятьсот` and `100%` -> `сто процентов`. `--include-comment-labels` can restore spoken "Comment by user" labels when desired.
- If `comment_tts_voice` differs from `tts_voice`, `translator_tts.py` now runs multi-voice TTS: title/body use the narrator voice, comment bodies use the comment voice, long text is chunked with `--tts-segment-max-chars`, and FFmpeg concatenates the MP3 segments. Current workflows request `--with-transcript`; transcript parsing/timing data is used for static slide boundaries while visual karaoke remains off. AI33 subtitle outputs (`subtitle`, `srt`, `vtt`, `subtitle_url`, `srt_url`, `vtt_url`, and base64 equivalents) are parsed into word-like cue timings before falling back to estimated timings. Retryable AI33 task errors during per-segment multi-voice generation are retried before the pipeline fails.
- `translator_tts.py --check-voice-config --require-voice-prefix elevenlabs_` validates narrator/comment voice ids without reading story text or calling AI33. `auto_publish.yml` runs this preflight before Reddit/Gemini/AI33 work.
- `translator_tts.py` now handles raw URLs and service link lines such as `Original thread: https://...` as narration-only substitutions. Storyboard/display fields keep the visible URL, while TTS reads localized phrases such as "el enlace está en pantalla" so videos show the link instead of printing the spoken placeholder.
- `storyboard_generator.py`, `app.js`, and `style.css` now render videos as centered slides rather than one long scrolling post. Story slides never include comments; comment slides show only comments; long story text is chunked into additional story slides. Render-mode typography now uses a Reddit-like system UI font stack, larger Shorts/16:9 text, wider cards, tuned slide character limits, and a subtle dark grain/vignette background so the card uses more of the frame without decorative clutter.
- `render.py` now auto-detects default `narration.mp3`, captures deterministic clean timed-slide frames by default, and merges the audio track into `final_output.mp4`. Current workflows pass `--no-karaoke` to disable visual highlights, but still generate and read `narration.json` when available. Render orientation is duration-aware: `<=180s` stays vertical 9:16, `>180s` becomes horizontal 16:9.
- Acc1 unlisted live run `28573148388` on 2026-07-02 succeeded through upload to `gZP4fiM5iTo` on `ChonkerTalks Россия` with `privacy=unlisted` and `language=ru`, but AI33 returned transcript metadata without complete word timings for a very long narrator segment, so `render.py` fell back to non-karaoke slide-progress before upload. Follow-up runs `28574532627` and `28576525232` stopped before upload because `auto_publish.yml` requires karaoke and AI33 task payloads still did not expose usable timings, even for a short 4-segment / 1718-character story. The follow-up fix now chunks long TTS narration, parses ElevenLabs character alignment when available, and uses estimated per-word timings from text plus segment audio duration when AI33 returns audio without timings. Acc1 unlisted run `28693621091` on 2026-07-04 verified that this path can pass `--require-karaoke` and upload with in-text karaoke enabled.
- `style.css` now hides editor/sidebar/HUD/desktop-nav/sidebar/safe-zone widgets under `.clean-mode` and `.render-mode`, including desktop layout states. The 16:9 render layout uses a wide clean Reddit card with horizontal-specific spacing so long-form output is not a vertical canvas with empty side filler. Render-mode background is now a restrained dark Reddit-like surface with subtle grain and vignette, not a bright decorative gradient. Local no-spend smokes verified clean vertical static slides, vertical fallback render with bad transcript, vertical slide karaoke render with gold word highlight, and horizontal desktop slide karaoke render with no control/sidebar/topbar widgets and AAC audio in the MP4.
- `.github/workflows/auto_publish.yml` and `.github/workflows/video_dry_run.yml` now pass `GOOGLE_GEMINI_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `VECTORENGINE_API_KEY`, and `AI33_API_KEY` to the relevant text/TTS steps. Google Gemini is preferred for text calls when the secret is configured; VectorEngine remains fallback and image provider.
- `.github/workflows/audit_voice_youtube.yml` is a manual audit workflow for two isolated checks: short AI33 voice samples through `scripts/generate_ai33_voice_samples.py`, and YouTube refresh-token mapping via `uploader.py --check-channel-only` for `acc1` through `acc7`. It does not fetch Reddit, call Gemini/VectorEngine, render video, or upload to YouTube. Its voice-sample path exposes `text_style`, `voice_settings_profile`, and an optional raw `voice_settings_json` override; start listening tests with `text_style=emotional` and `voice_settings_profile=creative`, then compare `natural` if the voice is too theatrical. Manual `auto_publish.yml` and `video_dry_run.yml` also expose `voice_settings_profile`, but scheduled runs remain on `default`.
- `scripts/issue_youtube_refresh_token.py` is the local helper for reissuing scoped YouTube refresh tokens. It runs a local OAuth consent flow for one `acc#`, requests `youtube.upload`, `youtube.readonly`, and `youtube.force-ssl` by default, and can store the resulting token directly into `YOUTUBE_REFRESH_TOKEN_ACC#` with `gh secret set` without printing the token.
- Audit run `28457170166` on 2026-06-30 generated all 14 AI33 voice samples successfully and uploaded the `ai33-voice-samples` artifact. The local review page is `build/audit/run_28457170166/ai33-voice-samples/20260630T154616Z/voice_samples_review.html`.
- Historical mapping-only audit run `28458168727` saved per-account logs under `build/audit/run_28458168727/youtube-mapping-acc*/acc*.log`. That pre-reissue run reached YouTube but failed before channel comparison with Google `403 insufficient authentication scopes`. Per current user-provided state, the seven tokens/scopes were reissued afterward and are no longer the active blocker.
- Historical scope-aware audit run `28459324708` confirmed the exact granted scope for the pre-reissue `YOUTUBE_REFRESH_TOKEN_ACC1-7` values: only `https://www.googleapis.com/auth/youtube.upload`. That explained why adding scopes in Google Cloud/OAuth configuration did not update the refresh tokens stored in GitHub Secrets. Per current user-provided state, the seven tokens/scopes were reissued after that audit.
- `scraper.py` now supports `--time auto`, `--topic-family`, `--max-ai-candidates`, `--candidate-limit`, `--similarity-threshold`, and `--producer-queue-output`. `auto` mode scans capped topic-family source plans rather than only `top/week`, applies channel `topic_exclusions`, then sends only the top bounded pool to Gemini. The topic-search prompt has been upgraded from a loose fit check to an outside-in producer gate: Reddit upvotes are evidence only, and Gemini must skip generic Reddit filler, screenshot/link-dependent posts, narrow-fandom posts, topics that cannot carry the requested format, and candidates likely to look like mass-produced AI content. Approved stories now carry packaging fields such as `content_bet`, `audience_job_fit`, `first_screen_promise`, `first_screen_text`, `packaging_thesis`, `why_now`, `shorts_cut`, and `longform_angle`; each candidate also needs exact source-backed `hook_evidence`. The scraper writes `producer_queue.json` and selects the best approved candidate by producer score instead of stopping at the first pass.
- `story_adapter.py` now runs in `auto_publish.yml` and `video_dry_run.yml` after scraper selection. It performs a source-backed no-invent cleanup, writes `editorial_adaptation` into `story_data.json`, and fails strict runs when the hook evidence quote cannot be found in source text. This is the boundary between raw Reddit source material and narrated script text.
- `metadata_generator.py` now generates three packaging options and stores them in `youtube_metadata.json.packaging_options` with a selected option index. The uploaded metadata remains backward-compatible through `youtube_title`, `thumbnail_text`, tags, hashtags, and SEO keywords.
- `scraper.py` also supports format-specific source-length selection through `--format-intent`. Production `shorts` runs now select complete short source stories before adaptation instead of cutting long posts after selection; current source-body policy is roughly `250-2400` chars for Shorts and `2800+` chars for long-form. The Gemini producer gate receives the full short-source body up to the Shorts limit, so it should not reject complete short candidates as "truncated" merely because the old prompt preview ended early. Scraper/adapter `--max-body-chars` is now a deprecated manual safety valve and is ignored unless `--allow-body-trim` is passed.
- `published_history.json` remains backward-compatible with the old `{post_id: [channels]}` shape; the next scraper save migrates future entries to versioned records with story signatures, keyword signatures, topic family, time window, velocity, fatigue penalty, virality score, and AI quality data.
- `.github/workflows/auto_publish.yml` supports manual topic strategy and content length selection and uses `privacy_status=unlisted` by default for manual dispatch. `content_format=auto` resolves to `long` for `acc1` and `shorts` for the other channels; scheduled `acc1` entries explicitly use `long`. Long selection preserves the full source and lets render auto-switch to 16:9 when audio exceeds 180 seconds. Explicit `shorts` for `acc1` uses the isolated source-backed trailer adapter. `video_dry_run.yml` follows the same format routing. Its live producer dry-run budget is capped at 12 Gemini candidates, metadata can use a marked fallback for artifact inspection, and both workflows remain fail-closed through adaptation, TTS/model, render, and pre-publish QA gates.
- `uploader.py` now supports `--check-channel-only`, `--privacy-status public|unlisted|private`, merges `tags` + `seo_keywords`, appends returned `hashtags` to the description, passes metadata language to YouTube, verifies the authenticated YouTube channel before upload, and reads back uploaded video metadata afterward. `auto_publish.yml` runs the channel check before any content-generation spend. Manual and scheduled runs should stay `unlisted` until one post-fix artifact is inspected end to end.

## Known Blockers

- No channel has retention/viewer proof for its new promise. Exact `topic_mix` weights remain unapproved; the 2026-07-11 committed-config reviews disproved the present mixes as publish selectors but did not establish replacement weights.
- AI33 A/B run `29153420437` generated `acc1` narrator/comment samples with `model_id=eleven_v3`, emotional text, and provider-default voice settings. Run `29153423227` used the same voices/text/model with creative settings `stability=0.35`, `similarity_boost=0.75`, `style=0`. Both runs succeeded; their YouTube mapping jobs were skipped, and no Reddit, Gemini, render, or YouTube upload path ran.
- The four downloaded MP3s are valid 44.1 kHz stereo, approximately 128 kbps. Default durations were 11.154s narrator / 5.799s comment; creative durations were 10.684s narrator / 6.034s comment. Manifests prove the requested `eleven_v3` contract but do not include a separate provider-echoed resolved model, so the honest verification level is requested-model plus successful synthesis, not independent server echo.
- The strategy reset, held-channel guard, network-wide dedupe, tests, and source-review workflow are pushed only on branch `codex/reddit-topic-source-review-20260711` at `ca6b0ad`. `origin/main` remains unchanged and does not enforce the new holds.
- Source-only runs `29073999465` through `29074038795` used commit `010fb95`, not the local revised `channels.json`. They therefore validate Reddit access and old-family source behavior only. Across 12 queues, only eight unique candidates appeared and four unique stories were selected; generic AITA repeated across `acc1/acc4/acc5/acc7`, while source-incomplete OutOfTheLoop questions repeated across `acc3/acc4/acc6`.
- Committed-config runs `29152777746` through `29152780145` used `ca6b0ad`, config SHA-256 `588466d4e6250328660a17f19fa8cb84462f82ccbca7934125d10b39184f56e1`, `AI_QUALITY_CHECK=0`, separate `week`/`month` windows, one subreddit per family, ten posts maximum per source, no history write, and no Gemini/AI33/render/YouTube. They produced 63 queue entries but only 19 unique posts; 28 entries were link-dependent and the same two posts won all 12 runs.
- `acc2` was not included in those reviews and still needs a dedicated high-competition competitor/source-lane review.
- The `evidence_dossier` source/script/render lane does not yet exist. This blocks `acc2`, `acc3`, `acc5`, and `acc6`, plus any factual case on another channel.
- The daily factory now implements producer+critic selection, source-preserving translation review, original packaging/scene direction, deterministic neutral metadata, and once-per-episode fiction/unverified disclosure locally. These contracts still need exact live artifact inspection and human originality review before any inauthentic-content/reused-reading risk can be considered cleared.
- `acc1` has no permission/license ledger or distribution rights gate. Public Reddit text is user-owned; attribution and Reddit upvotes do not grant commercial adaptation rights. The approved artifact-only runs listed above were bounded technical exceptions and remained fail-closed against upload; any distribution remains blocked until the rights gate and completed human greenlight exist.
- Scheduled runs `29070128277` and `29072898300` were verified no-ops: each completed only `resolve`, while `publish` was skipped. The scheduler derives routing from the runner's actual UTC hour, so delayed GitHub cron starts can miss the intended matrix hour. This is an operations blocker, not a content success.
- GitHub Reddit source access itself is healthy: isolated source-only run `29069048314`, all twelve 2026-07-10 review runs, and the seven exact-pilot/readback runs on 2026-07-14 passed credential preflight and bounded PRAW access. The pilot 01/02 failures proved that the historical one-source SAGA design was unsuitable; the current local redesign uses BUNDLE and has not yet been live-verified. The local terminal still needs explicit `REDDIT_*` environment values.
- Direct Google Gemini text routing is implemented and verified in dry-run `28703717316`: metadata and localization artifacts reported `source=google-gemini`. The key that was pasted into chat should still be treated as exposed and rotated before production use.
- `scripts/move-to-trash.sh` is the project safe-trash helper; generated scratch artifacts should be moved through that workflow, not direct deletion commands.

## Next Steps

1. Do not assign numeric `topic_mix` weights yet. The six slots are comparable pilots, not proven audience weights.
2. Verify that GitHub `main` contains the exact reviewed PR #5 Flex revision. Only after a separate exact provider-spend approval, dispatch one bounded `acc1_daily_episode.yml` canary; preserve the run ID and download/read back the full artifact. A successful job proves production plumbing only, not audience demand.
3. Inspect the winning sources, both 90/100 reviews, fiction/unverified disclosure, translation audit, male/female role routing, thumbnail, complete MP4, Reddit-page pacing, cat-safe visual boundary, and QA hashes. Any failed gate blocks release rather than falling back to generic content.
4. Complete human creative and rights review on that exact hash-bound artifact, then use the active no-provider release-review workflow to create an exact private-review receipt. YouTube upload remains a separate authorization.
5. Keep publishing disabled until an accepted branded artifact exists; then run a separately approved unlisted pilot and collect comparable 24h/72h/7d/28d retention/engagement readback before changing weights.
6. Build one deterministic `evidence_dossier` fixture with fact/claim/theory separation and timeline/evidence visuals before testing `acc2`, `acc3`, `acc5`, or `acc6`.
7. Fix scheduled routing so delayed cron starts map from `github.event.schedule` rather than the runner's current UTC hour, while keeping all channels fail-closed.

## DESIGN.md Agent Bridge

Installed 2026-07-08: project-root `DESIGN.md` is now available as an agent-facing visual/design brief using the plain Markdown DESIGN.md pattern from https://github.com/VoltAgent/awesome-design-md. It is a bridge for UI/visual work and does not supersede the existing source-of-truth docs listed in `docs/README.md`.
